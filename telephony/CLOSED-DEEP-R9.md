# סגירת נחיל-עומק · סבב-9 (R9)

**תאריך:** 2026-08-14 · **ענף:** `claude/telephony-engine` · **בדיקות:** 667 → **677** (‏0 נכשלו) · **golden:** ללא-שינוי (התיקונים לוגיקה/CLI, לא נוגעים בפלט-הפיקסצ׳רים).

נחיל-עומק סבב-9 (‏14 סוכנים · 8 ממדים · דגש עמידות-ICU) אימת את R8 **חיובית** (normMonth מלא — תמוז הוא החריג-היחיד; R8-3 השכתוב נכון, אין edge-חדש; אין "תמוז שני"). **פסק-דין: R8 החזיק — אך המנוע לא התכנס:** ‏4 ממצאי-אמת, כולל **רגרסיה בתיקון-R8-5 שלי** ו**חבות-apply-cli** (הכלי-"המרכזי" משתיק חגים + עוקף שערים). כולם תוקנו עם ratchet.

## 🔴 קריטי · חבות-כלי-מפעיל

| # | ממצא | קובץ | תיקון |
|---|------|------|-------|
| **R9-1+5** | **[HIGH]** `apply-cli.mjs` (README: "כלי-מפעיל מרכזי") בנה **בלי anchorDate** ⇒ סגירות-חג/זמנים נעלמו בשקט (עונה ביום-כיפור), ו**חסר את כל 4 שערי-הבטיחות** של `tel.mjs apply` (auditRoutes · crossTenantLeakScan · secretPreflight · migrationRisk pre-pass). התפצל היסטורית מ-tel.mjs וצבר סחף | `apply-cli.mjs` + README | **אוחד** — apply-cli הפך ל-shim שמנתב ל-`tel.mjs apply` (מקור-אמת יחיד). אומת: כעת מזריק anchor (13 heb_ במקום 0) + מריץ השערים |

## 🟠 נכונות

| # | ממצא | קובץ | תיקון |
|---|------|------|-------|
| **R9-2** | **[רגרסיית-R8-5]** הקצאת-ערוץ חד-מעברית (`Math.max`) — auto-**לפני**-explicit נתן **ערוץ-כפול בשקט** (`[{auto},{ch:1}]`→שניהם 1) ⇒ SIM בלתי-נגיש (FreeSWITCH בוחר ראשון), ובמצב-כשר יציאה דרך SIM לא-מכוון | `onboard.mjs` + `validate.mjs` | הקצאה **דו-מעברית** (Set-שמור אז דילוג-אוטומטי) + **שער-מדדף `gatewayChannel`** ב-validate (error, כמו id/e164) — תופס גם קונפיג יד-כתוב |
| **R9-3** | **[משפחת-"תמוז": הגנרטור מכבד שדה שהוולידטור זורק]** `validate` מחק את בלוק `security` ⇒ `generate` קרא `tenant.security.maxConcurrentOut` undefined ⇒ **חצי-תקרת-ה-toll-fraud של `voice.hardening` no-op** (ברירת=sims.length=המגבלה-הפיזית) | `validate.mjs` | `security` מנורמל (allowlist maxConcurrentOut/maxCallSec/recordingEncryption) מועבר ל-tenant |
| **R9-4** | יעד-IVR `dest.type='number'` גישר החוצה דרך השער **בלי tollGuard** (בעוד out_default/out_pick/out_intl/sd_ כולם מוקשחים) — מחלקת-ה-toll-fraud ש-R6-5 סגר ל-sd_ | `generate.mjs` | `tollGuard` מושחל ל-`destActions` case 'number' |

## אימות R8 (החזיק — הנחיל אישר מפורשות)
- **R8-1 (normMonth):** מנייה-מלאה של 14 שמות-חודשי-ICU (שנה מעוברת) — **רק `Tamuz` סוטה**; ימות-השבוע ממופים + נפילת-`getUTCDay` עמידה; **אתר-Intl-עברי יחיד** ⇒ אין "תמוז שני".
- **R8-3 (zmanimClosedReason):** נכון — מפעיל את **אותה** `hebrewClosedWindows(anchor,window)` כמו הגנרטור ⇒ קבוצת-חלונות ביט-זהה; אופק-יום-מלא w-1 תואם. אין edge-חדש. (‏400 חלונות/קריאה — כלי-תצוגה, לא מסלול-חם.)
- **R8-2/R8-4** — החזיקו.

## אינווריאנטים שנשמרו
- **‏golden byte-identical** — כל 4 התיקונים לוגיקה/CLI/validate; ‏R9-4 מוסיף tollGuard **רק** כש-hardening דלוק (פיקסצ׳רים בלי-features ⇒ 0 שינוי). ‏0 שינויי-golden.
- **דגל-כבוי = ביט-זהה · pure-downstream · CTI קריאה-בלבד · אפס-קבלות · דטרמיניזם** — נשמרו.
- **מקור-אמת יחיד** — apply-cli אוחד ל-tel.mjs (אין עוד שני-מסלולי-החלה מתפצלים).

**מצב:** ‏677/677 ירוק. **מגמה:** R4→R9 = 19→18→13→9→8→**4** ממצאים. הממצאים מתכנסים אל **ליטוש-מבני** (רגרסיות-עצמי, כלי-שהתפצל, שדות-שהוולידטור-זורק) יותר מבאגי-לוגיקה-חדשים. סבב-10 מאמת את R9 (בפרט האיחוד + security-passthrough) כבדיקת-התכנסות אחרונה.
