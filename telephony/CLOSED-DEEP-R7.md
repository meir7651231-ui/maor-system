# סגירת נחיל-עומק · סבב-7 (R7)

**תאריך:** 2026-08-14 · **ענף:** `claude/telephony-engine` · **בדיקות:** 646 → **658** (‏0 נכשלו) · **golden:** ללא-שינוי (התיקונים לא נוגעים בפלט-הפיקסצ׳רים).

נחיל-עומק סבב-7 (‏28 סוכנים · 9 ממדים) אימת את R6. **פסק-דין: R6 החזיק ב-10/13 — המנוע *לא* התכנס.** ‏16 ממצאים אושרו → **9 ייחודיים**, ובהם **באג-אמת אחד חוסם-deploy (🔴 HIGH)** — פער-השלמה בתיקון R6-9 שלי. שלושה מתיקוני-R6 נשארו לא-סגורים-עד-הסוף, ושני חורים-קדומים נחשפו (אטומיות-apply, סתירת-אורקלים). כל 9 תוקנו עם ratchet/smoke.

## 🔴 קריטי · חוסם-deploy

| # | ממצא | קובץ | תיקון |
|---|------|------|-------|
| **R7-1** | **פער-R6-9:** ‏`esc` (לא `escc`) בהערת `out_default` — תווית-SIM עם `--` מפילה את **כל הדיאלפלן** (‏expat דוחה) ⇒ המרכזייה הקולית לא נטענת (‏`buildTenant` עדיין `ok:true` — אף ולידטור לא תופס `--` בתוויות). R6-9 כיסה 4 משטחי-הערה, פספס את החמישי | `generate.mjs:568` | `escc(def.label)` + ratchet source-guard סורק את כל ה-XML ל-`<!--…--…-->` פסול |

## 🟠 נכונות / אינווריאנט

| # | ממצא | קובץ | תיקון |
|---|------|------|-------|
| **R7-2** | סיבת-סגירת-צום (מסלול לא-zmanim) קרסה ל-boolean `true` (האופרנד-האחרון `!c.shabbat`) ⇒ `explainCall` הציג "🌙 מחוץ-לשעות (true)" | `simulate.mjs:179` | הקדמת `!c.shabbat` לפני `c.fast` ⇒ המחרוזת (שם-הצום) שורדת |
| **R7-3** | `apply` רב-דיירת **לא-אטומית** — שער `migrationRisk` ההרסני ישב *בתוך* לולאת-הכתיבה ⇒ `die()` באמצע השאיר דיסק מעודכן-חלקית + STATE ישן | `tel.mjs` | הועבר ל-**pre-pass** לפני לולאת-הכתיבה (כמו יתר השערים) — die יחיד ואטומי |
| **R7-4** | `complianceReport` הצהיר `recordingEncryption:true`/`compliant:true` על-דגל בלבד — סתר את R5-F4 (‏`trustReport` מדווח דורמנטי) | `security.mjs:158` | יושר ל-"מוגדר-אך-דורמנטי" — `recordingEncryption:false` + `recordingEncryptionConfigured` + ממצא |

## 🟡 חיזוק / correctness-risk (בהם 3 השלמות לתיקוני-R6)

| # | ממצא | קובץ | תיקון |
|---|------|------|-------|
| **R7-5** | **פער-R6-1:** ערוץ-שער שלילי (‏`gatewayChannel=-1`) בנה `<id>-gw-1` שה-`-` בו עיוור את `crossTenantLeakScan` (העוגן-הימני `(?=\d)`); הוולידציה בדקה `Number.isInteger` בלי `>0` | `validate.mjs` | דרישת `gatewayChannel > 0` (מונע ערוץ-שלילי מלכתחילה) |
| **R7-6** | **פער-R6-8:** `withinHorizon` (`date<=anchor+window`) מול מסלול-היום-המלא (`i<windowDays` ⇒ עד `+window-1`) ⇒ off-by-one ביום-הקצה. וכיוון-הפוך במסלול-zmanim (ריצה עד `+2`) | `simulate.mjs:54` | גבול תלוי-מסלול: יום-מלא `+window-1`, zmanim `+window+2` |
| **R7-7** | **פער-R6-4:** הפילטר-הקולי לא החריג `forwardTo` alias (הגנרטור מדלג עליו) ⇒ `in:<alias>` שגוי | `simulate.mjs:132` | `&& !(customer-forward && forwardTo)` |
| **R7-8** | **פער-R6-6:** `sig` מיפה רק `id:e164` (לא `gatewayChannel`) ⇒ החלפת-ערוצים בין שני SIM (ניתוב-פיזי אחר) עברה בשקט | `apply.mjs:231` | `gatewayChannel` נכלל ב-sig ובהשוואת-survivor |
| **R7-9** | **פער-R6-2:** reuse-עוגן ללא-הסלמה — אחרי `calendarWindow` ימים כל סגירות-החג "מתאדות בשקט" (חזרה-איטית ל-R5-F5); הרמז לא-פעל | `tel.mjs:98` | **רענון-אוטומטי** כשכיסוי-העתיד `<60` יום (עולה פעם ב-~שנה, לא יומי ⇒ אידמפוטנטיות נשמרת) |

## אינווריאנטים שנשמרו
- **‏golden byte-identical** — כל 9 התיקונים אינם נוגעים בפלט-הפיקסצ׳רים (`escc(def.label)` שונה רק כשיש `--`; יתר התיקונים ב-simulate/apply/tel/validate/security — לא golden-XML). ‏0 שינויי-golden.
- **דגל-כבוי = ביט-זהה · pure-downstream · CTI קריאה-בלבד · אפס-קבלות** — נשמרו.
- **אורקלים מסכימים** — `trustReport`≡`complianceReport` על הצפנת-הקלטות (R7-4). **אטומיות-apply** אומתה end-to-end (שינוי-הרסני ⇒ die לפני כל כתיבה, קובץ-שכן ביט-זהה). **רענון-עוגן** אומת (עוגן-ישן ⇒ ♻️ ל-today).

**מצב:** ‏658/658 ירוק. הלולאה ממשיכה — סבב-8 מאמת את R7 וחופר עמוק יותר. **מגמה:** R4→R5→R6→R7 = 19→18→13→9 ממצאים, החומרה יורדת (סבב-7: HIGH-אחד, שהוא פער-השלמה בתיקון-עצמי). ההתכנסות מתקרבת אך טרם-הושגה.
