# סגירת נחיל-עומק · סבב-8 (R8)

**תאריך:** 2026-08-14 · **ענף:** `claude/telephony-engine` · **בדיקות:** 658 → **667** (‏0 נכשלו) · **golden:** עודכן-מכוון (R8-1: הוספת צום-תמוז — ‏4 קבצים, ביט-מאומת).

נחיל-עומק סבב-8 (‏17 סוכנים · 8 ממדים · כוונון-מחמיר "עדיף-נקי") אימת את R7 ובדק-התכנסות. **פסק-דין: R7 החזיק (0 רגרסיות מבניות) — אך המנוע *לא* התכנס: R8 חשף באג-אמת שנוגע לגנרטור-עצמו וששרד את כל 4 הסבבים הקודמים.** ‏8 ממצאים → **5 ייחודיים**, כולם תוקנו עם ratchet/smoke.

## 🔴 קריטי · באג-אמת (שרד 4 סבבים)

| # | ממצא | קובץ | תיקון |
|---|------|------|-------|
| **R8-1** | **[HIGH · פוגע בקונפיג-המחולל]** ICU (Node22/ICU78) פולט את חודש תמוז כ-`Tamuz` (מ׳ יחיד), אך הקוד השווה `Tammuz`. תוצאה מאומתת: `classifyDay('2026-07-02')` (י״ז בתמוז) → `fast:null` ⇒ **הדיאלפלן לא סוגר את הצום כל שנה** + `"ראש חודש Tamuz"` דלף לתווית-עברית. אימות-רוחב: תמוז הוא **החריג היחיד** מבין 14 החודשים | `hebcal.mjs` | נרמול `Tamuz→Tammuz` **מיד** אחרי ICU ב-`hebParts` — נקודה-אחת שמתקנת גם את הצום-שנשמט וגם את דליפת-הלטינית, ועמידה לכל גרסת-ICU |

## 🟠 נכונות / רגרסיה (כלי-מפעיל)

| # | ממצא | קובץ | תיקון |
|---|------|------|-------|
| **R8-2** | `apply-cli.mjs` prune לא דחף ל-`touched.deletes` ⇒ כשפירוק הוא השינוי-היחיד, `reloadPlan([])` ריק ⇒ אין הוראת-reload ⇒ FreeSWITCH ממשיך לענות בדיאלפלן-הרפאים | `apply-cli.mjs:100` | `touched.deletes.push(rel)` (כמו `tel.mjs:163`) |
| **R8-3** | **פער-R7-6:** `withinHorizon` סקלרי לא-יכול לשקף את **קבוצת-החלונות** של הגנרטור — 3 מקרי-קצה: צום-יום-מלא בזנב w+2, חלון שמתחיל מעבר-לאופק, ו**ערב ב-anchor-1 כשהאנקור שבת/יו״ט** (הכיוון-המסוכן: אורקל-פתוח מול PBX-סגור) | `simulate.mjs` | **תיקון-שורש:** `zmanimClosedReason` מחשב את קבוצת-החלונות **בדיוק כמו הגנרטור** (מהאנקור) ובודק חברות-בפועל; נפילת-היום-המלא בגבול w-1 נפרד. מנקה את שלושת המקרים |

## 🟡 חיזוק

| # | ממצא | קובץ | תיקון |
|---|------|------|-------|
| **R8-4** | `zmActive` בסימולטור מגודר `call.date`, `zmanimOn` בגנרטור מגודר `opts.anchorDate` ⇒ build בלי-anchor פולט חיתוך-שישי סטטי **בשקט** (‏warnings:[]) בעוד הסימולטור מחשב חלון | `generate.mjs` | אזהרת `calendar.zmanim בלי anchor` (מקבילה לזו של `calendar.hebrew`) |
| **R8-5** | `tenantFromIntake` מתעלם מ-`gatewayChannel` שהמפעיל סיפק ב-CSV (עמודה = קלט-מת) ⇒ שני-מסלולי-הכניסה מתפצלים | `onboard.mjs:107` | לכבד ערוץ-חיובי-שסופק לפני הקצאה-אוטומטית; המונה מדלג מעליו |

## אימותי-R7 (החזיקו — הנחיל אישר מפורשות)
`R7-1` (escc על כל משטחי-ההערה) · `R7-3` (pre-pass, next/touched עקביים) · `R7-4` (recordingEncryptionConfigured) · `R7-5` (ערוץ>0) · `R7-9` (רענון-אוטומטי = **התנהגות-רצויה**, reloadxml פעם-ב~שנה) — כולם נקיים, 0 רגרסיות.

## אינווריאנטים שנשמרו
- **‏golden** — השינוי היחיד (R8-1) אומת ביט-אחר-ביט: **בדיוק 7 צומות-תמוז נוספו** (‏2024–2030) ל-`luach-known`, **0 הוסרו**; `golden-kollel`/`verticals` קיבלו את צום-תמוז-2027 בלבד (heb_ + manifest). אפס שינוי אחר.
- **דגל-כבוי = ביט-זהה · pure-downstream · CTI קריאה-בלבד · אפס-קבלות · דטרמיניזם** — נשמרו.
- **אטומיות + reload** — apply-cli prune אומת (reloadxml נפלט end-to-end).

**מצב:** ‏667/667 ירוק. **מגמה:** R4→R8 = 19→18→13→9→8 ממצאים. R8 הוכיח ערך-הלולאה — באג-תמוז אמיתי (סגירת-צום-שגויה כל שנה) ששרד 4 סבבים. הלולאה ממשיכה — סבב-9 מאמת את R8 (בפרט תיקון-השורש של withinHorizon) וחופר עמוק יותר.
