# ✅ CLOSED — לולאת-האמון (TRUST, 24.8.2026)

**המקור:** ביקורת-עומק של 6 סוכני-חקירה (~70 טענות מול הקוד, ראיות file:line)
שבדקה מסמך-ביקורת חיצוני. ~40% מהביקורת התבדה (קיים ומאומת), ~35% חסר באמת —
אבל הבדיקה חשפה 9 חורים קונקרטיים שהביקורת כלל לא ראתה. הלולאה סגרה את כולם.

## 8 האשכולות שנסגרו (PR ‏#402–#417, כולם squash-merge ל-main)

| # | PR | מה נסגר |
|---|---|---|
| 1 | ‏#402 | **אטימת-PII**: דוח-המשפחה הדפיס ת"ז+רגישויות גם כשהדגלים כבויים (עוקף-גידור) — עכשיו מכבד `families.showid`/`families.health`; שער "רק-מנהל-מנפיק" הורחב מ-D- גם ל-R- (‏S- פתוח במכוון — לא מסמך-מס); יומן-גישה `logAccess` (חשיפת-ת"ז · הורדת-דוח · ייצוא-גיבוי) |
| 2 | ‏#403 | **הקשחת-נעילה**: PIN מ-SHA-256-חד-סיבובי ל-PBKDF2 ‏310K + מלח-אקראי (`v2:{salt}:{hash}`; לגאסי נפתח ומשודרג-בשקט); מונה-כשלונות שורד-רענון; הגבלת-קצב במסכי פתיחת-ההצפנה (מקומית+ענן) |
| 3 | ‏#404 | **מצבות-מחיקה**: `Db.delLog` (טבעת-500, רוכבת על meta) — setDb מטביע מצבה מרכזית; איחוד-ההתחברות מסנן לפיהן ⇒ מחיקה לא קמה לתחייה ממכשיר-אופליין; מיזוג-מצבות=איחוד (לא ענן-מנצח) |
| 4 | ‏#405 | **בריאות-outbox**: מסך 📤 בהגדרות←אבטחה (ענן+מנהל) — כשלי-SMS/מייל גלויים + 🔁 שליחה-חוזרת; חיווי-סנכרון `pending` (נקודה כחולה = שינויים שטרם נדחפו) |
| 5 | ‏#406 | **CI-אבטחה**: ג'וב `rules` (red-team מול אמולטור בכל push) + `npm audit` מדווח + `dependabot.yml` שבועי |
| 6 | ‏#416 | **עמידות-הקופה**: שיקוף-כפול — localStorage נשאר מקור-העבודה, כל כתיבה משתקפת ל-`db.ui.cash*` ⇒ גיבוי/סנכרון/צילומים; ריפוי-מ-DB + מונה-מקסימום (אין כפל-מספור) |
| 7 | ‏#417 | **חותמות-הסכמה**: `Member.mConsentAt` — תאריך פר-הסכמה, נחתם בהדלקת-צ'יפ, מוצג בכרטיס |
| 8 | (הענף הזה) | **טיוטת הגנת-קבלות ב-Rules**: עדכון/מחיקה על `orgs/{slug}/donations` = מנהל/מייל-על בלבד (החור: חבר-מוגבל יכול היה לעדכן/למחוק קבלת-§46 בייעוד-שלו). ‏red-team הורחב ל-**63 תרחישים, 63 ירוקים** |

ratchets חדשים: `report-pii-gate` · `lock` (מורחב v2/לגאסי) · `del-tombstones` ·
`outbox-health` · `cashbox-durable` · `consent-stamp` + 5 תרחישי-red-team.

## ⚠️ חלונות-בעלים (בנוי — ממתין לך)
1. **פרסום ה-Rules המעודכנים** — הטיוטה בריפו עברה 63/63; הפרסום לפרודקשן =
   `firebase deploy --only firestore:rules` (או קונסולה). עד אז החור פתוח בפרודקשן.
2. **הפעלת הצפנת-הענן** — בחירת-סיסמה + מיגרציה (Runbook קיים).
3. **MFA** — דורש הפעלת TOTP בקונסולת-Firebase + חיווט (טרם נבנה — roadmap).

## 🧮 שאלות פתוחות לרו"ח (גבול-תפקיד — לא מתקנים בעיוור)
- **append-only מלא לקבלות?** הטיוטה = מנהל-בלבד; ההחמרה (`allow update, delete: if false`) מחייבת קודם מסמך-זיכוי ל-D-/R- שטרם קיים.
- **מסמך-זיכוי/ביטול** ל-D-/R- (לחנות יש voidedAt; לתרומות אין).
- **תווית "חשבונית" בקופה-הרושמת** — המסמך אינו חשבונית-מס; לשנות ל"אישור-תשלום" (תקדים ה-S-)?
- מיזוג חיוב-סולה: hist-בלבד או קבלת-D (פתוח מ-CLOSED-SOLA).

## 📦 Dependabot
הפעלת `dependabot.yml` פתחה מיד גל PRs ‏#407–#415 (עדכוני-תלויות). **לא מוזגו
במכוון** — כל אחד עובר את שער-ה-CI; מיזוג = החלטה מפורשת (רצוי אחרי בדיקת
changelog לתלויות-ריצה: react/zustand/firebase/idb).

## מה נשאר מחוץ-לתחום הלולאה (מהביקורת, מתועד ולא-נבנה)
ניהול-סשן/ניתוק-מכל-המכשירים · CSP/HSTS (GitHub Pages לא תומך headers — רק meta-CSP, נבחן בנפרד) · מדיניות-שמירה/DSAR מלא · חיוב-SaaS · SSO · ריבוי-שפות · i18n — ראו VISION-AUTOPILOT + ROADMAP-100.

## הבא בתור
‏task #45 — אוטופיילוט שלב-1 (לפי `knowledge/VISION-AUTOPILOT-2026-08-24.md`):
אמנת-סמכות → מנהל-בוקר-מבצע → דוח-השפעה.
