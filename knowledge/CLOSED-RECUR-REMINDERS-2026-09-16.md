# 🔁 בחירת-חזרה בכפתור ה-40 יום — סגירה (16.9.2026, ‏#496)

**בקשת-בעלים:** "תתן אותו אם אפשר בחירה האם חזרה יומי שבועי חודשי או 40 יום".

## מה נבנה
- **בורר-חזרה** בראש כרטיס-התורם (מעל הכפתור הענק): 🕯 40 יום · 📅 יומי (×30) · 📆 שבועי (×12) · 🗓 חודשי (×12) + שדה **כמות** (1–365) לסדרות החוזרות. ברירת-מחדל 40 יום ⇒ הסגולה **ביט-זהה**.
- **מנוע** (`supporters/lib.ts`): `RECUR_MODES` (מפתח · אימוג׳י · תווית · תחילית-הערה · כמות-ברירת-מחדל) · `recurDates(start, mode, count)` — יומי +i, שבועי +7i, חודשי +i חודשים עם **קיצוץ לסוף-חודש** (31.1 ⇒ 28.2 ⇒ 31.3) · `recurStatus(events, spId, today, mode)` — סגולה מאצילה ל-`segulaStatus`; שאר הסדרות מזוהות בתחילית משלהן (`חזרה יומית/שבועית/חודשית`) ⇒ **סדרות חיות במקביל**, ביטול אחת לא נוגע באחרת · `stripRecurNote`.
- **סטור:** `seedSegulaReminders(supId, start, purpose, mode='segula', count)` · `cancelSegula(supId, mode)` · `restartSegula(…, mode, count)` — הרחבת-חתימה בלבד. אירועי-לוח `call` עם `spId`, כותרת `📆 תזכורת שבועית — שם · i/N · זיווג`, קשר-הבא מקבל שורת-סדרה ויעד = הראשונה.
- **כרטיס:** פאנל ענק (64px/20px) לכל סדרה פעילה — "תזכורת i מתוך N · סיום dd/mm", צ׳יפים ✓, 🔄 התחלה-מחדש, ✖ ביטול (דו-לחיצתי); כפתור-זריעה למצב הנבחר; נעלם בזמן שהסדרה פעילה.
- ratchets: `recur-reminders.test.ts` (8) · `e2e/recur-reminders-smoke.mjs` (14/14) · `segula-smoke` 16/16. שני ratchets ישנים עודכנו לכוונה (חתימה מורחבת; ≥2 פסים).

## הפעלה
כרטיס-תורם ← שורת "חזרה:" ← בוחרים מצב (ואם צריך כמות) ← הכפתור הענק זורע. הסגולה נשארת בכפתור "🕯 40 ימים — התחלת סגולה לזיווג מהיום".
