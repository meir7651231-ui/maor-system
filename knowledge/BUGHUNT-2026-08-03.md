# דוח ציד-באגים — לקראת מסירה למאור (3.8.2026)

**מנדט-בעלים:** "צא לצוד באגים/קריסות/טעויות/שגיאות הכי רחב שיש … 99 זה כמו אפס."
**שיטה:** 8 צוותי-ביקורת מקבילים, כל אחד על ממד; כל ממצא **אומת אדוורסרית** מול
הקוד לפני תיקון (ממצא ≠ באג עד שהוכח). baseline: 1074 טסטים ירוקים.

## תוצאה: 13 באגים אמיתיים תוקנו · 2 נדחו-בשקיפות · הרבה אזורים אומתו נקיים

---

## תוקן והופץ (PR #47)

### 🟠 חמורים (5)
| # | קובץ | הבאג | תיקון |
|---|------|------|-------|
| 1 | `lib/hebrew.ts` hebAnnualEq | אזכרה/יום-הולדת ב-**30 אדר-א' נעלם ב-12/19 שנים** (שנה פשוטה) — כלל ל׳ השווה `prev==='Adar I'` שלא קיים ברצף שנה-פשוטה | נופל על א' ניסן (התנהגות מתועדת) · ratchet 5787/5788 |
| 2 | `store/useApp.ts` restoreDb | **shopReceiptSeq לא הורם ב-clamp** → אישור S- כפול חוצה-מכשירים אחרי שחזור-גיבוי | נוסף `Math.max` כמו R-/D- |
| 3 | `courses/CourseDetail.tsx` doPunch | הכרטיס עקף `setPresent` (#6) → **ניקוב-כפול חוצה-מסכים** (כרטיס↔יומן) + לחיצה-כפולה + מונה-חודשי 0 | נותב ל-`setPresent` האידמפוטני |
| 4 | `courses/ManageModal.tsx` | `used` (כל-הזמן) הוצג כ"נוכחויות מתחילת החודש" | `presentsInMonth` |
| 5 | `platform/lib.ts`+`ManagerPanel` | `removeMember` + `merge:true` → **כרטיס-עובד לא נמחק מהענן**, חזר באישור-מחדש + PII שנשאר | `deleteOrgMemberConfig` (FieldPath+deleteField) |

### 🟡 בינוניים/קלים (8)
| קובץ | הבאג | תיקון |
|------|------|-------|
| `wall/wallData.ts` buildWeek | חוגים הוסתרו בכל חג — כולל קלים (חנוכה/פורים/חוה"מ) | רק חג-מלא/שבת (`blockReason 'course'`) |
| `timer/MoneyTimer.tsx` | "גבייה היום" לפי חצות-UTC → שגוי בחלון-הלילה | `isoToday`/`isoLocal` מקומי |
| `store/persist.ts` migrate | `usdRate=0/null` בגיבוי מושחת אִפֵּס ערך-דולר (סכום/דירוג/ייצוא) | ריפוי לברירת-מחדל חיובית · budget שלילי→0 |
| `lib/cloud-diff.ts`+`merge.ts` | `budget`+`usdRate` נשמטו מסנכרון-הענן → סטייה בין-מכשירים | נוספו ל-META_KEYS/metaOf/assign |
| `wall/wallData.ts`+`reports` | `amount` לא-finite (גיבוי מושחת) → **NaN בקיר-ההשפעה/דוחות** | `Number.isFinite` |
| `courses/AbsenceModal.tsx` | חסר `justified`/`makeupEligibility` → זכאות-השלמה שונה מהיומן | אוחד עם `DiaryAbsenceModal` |
| `firestore.rules` orgMember | `.data.members` בלי ברירת-מחדל → נעילה אם השדה חסר | `.get('members', [])` |
| `ManagerPanel` clipboard · `CustomExport` notesEdit | rejection לא-מטופל · הערות ממופתחות לאינדקס-שורה | `catch` fallback · איפוס על שינוי |

---

## נדחה בשקיפות (לא-שקט)
- **⚠️ Rules catch-all `/{document=**}`** מעניק ל-`allowedRoot` **כתיבה חוצת-ארגונים** (Firestore = OR של כל הכללים; הרקורסיבי תופס גם `platformOrgs`/`orgs`). **דורמנטי היום** כי `allowedRoot`==מייל-על יחיד. הוספה **אזהרת-מקור בקובץ**. 🔴 **לפני** הוספת מייל לא-על ל-`allowedRoot`, או הכנסת ארגון-ענן שני — יש לצמצם את הכלל לאוספי-השורש בלבד (או להעביר נתוני-שורש תחת קידומת). שכתוב-כלל על הלקוח החי בלילה שלפני מסירה = סיכון גבוה מדי; מתוכנן כצעד-המשך מבוקר.
- **S- dedup דטרמיניסטי** (🟡): `seenS` תלוי-סדר-מערך (מול `planRidRenumber` הדטרמיניסטי של R-/D-). נדחה — נוגע ב-`migrate()` הרגיש, השפעה שולית (אישורים סמליים, מרוץ רב-מכשירי). fix מאותה מחלקה זמין.
- **memberConfigs = הרשאה ברמת-ממשק** (לא Firestore) — אילוץ מסמך-יחיד, מתועד ומכוון.

## אומת נקי (לא נמצאו באגים)
- **קונפיג/termOf** — סבב ה-~150 עטיפות נקי לגמרי (כל 12 הספריות עם המשמר האחיד; fallback ביט-זהה).
- **קריסות-UI / לקוח-ריק** — נתיב מאור-מתחילה-ריקה מוגן בכל מקום (arr[0]/find/reduce/JSON.parse/חלוקה-ב-0).
- **CSV injection** — סגור מלא (`csvEscape`/`esc` על כל נתיב; אין `join(',')` עוקף).
- **בידוד כספי** — R- רק ב-addPayment, D- רק ב-addDonation; tzedaka/shop לא זולגים; SHOP9 החריג היחיד המכוון.
- **מיגרציה** — idempotent (ריצה-כפולה לא מכפילה); שחזור/גיבוי/מולטי-טאב/quota עמידים.
- **לוח עברי** — נרמול-אדר, כלל-ל׳ לחשוון/כסלו, חנוכה, תשעה-באב-נדחה, gematria — כולם נכונים.
- **קבלות-מס** — {ok,rid} threading נכון; planRidRenumber דטרמיניסטי; hebrewNumber (אגורות/סמיכות) תקין.

**אימות:** 1077 טסטים · lint 0/0 · שלוש סוויטות דפדפן · הלקוח הקיים ביט-זהה. הופץ ואומת-ביט מול הפרודקשן.
