# CLOSED-SHOP2 — דוח מסירה · שדרוגי החנות (מלאי · אישור תשלום · תוקף קופונים)

**תאריך:** 2026-07-30 · **ענף:** `claude/what-do-you-see-bcxttj` · **מנדט:** `knowledge/BUILD-ORDER-SHOP2-2026-07-30.md`

## שורת סיכום
כל 4 האשכולות בוצעו לפי הסדר (קומיטים `חנות 9·`–`חנות 12·`). בדיקות 730 → **742** (0 אדומות) · שלוש סוויטות הדפדפן ירוקות כולל ההרחבה (מלאי 3 → "נותרו 2" · אישור S-1) · **סדרות R-/D- ללא נגיעה — מוכח ב-ratchet** · כל בדיקות הקופות והחנות הקיימות ירוקות (עדכון מבוקר אחד, מתחייב מהפקודה — ראו סטייה 1).

## טבלת פריט-מול-סטטוס

| # | פריט הפקודה | סטטוס |
|---|---|---|
| 1-2 | `ShopComponent.stock?` + ריפוי מיגרציה (לא-סופי מוסר, שלילי → 0) | ✅ `חנות 9` |
| 3 | `componentRemaining` (צריכה על-פני כל שיוכי המוצר, קטום ב-0, null בלי מעקב) + `stockOut` ב-needsCare (מוצר active) | ✅ `חנות 9` |
| 4 | UI: שדה מלאי בטופס; צ'יפ "נותרו N" ירוק/כתום/אדום בקטלוג ובשורת הרכיב; אזהרה רכה "המלאי אזל" במימוש (לא חוסם) | ✅ `חנות 9` |
| 5 | בדיקות: שני שיוכים מאותו מלאי, null, קטימה, stockOut, ריפוי | ✅ `חנות 9` (3 בדיקות) |
| 6 | `Db.shopReceiptSeq` (ברירת 1, "אינו קבלת מס") + `ShopRedemption.rid?` | ✅ `חנות 10` |
| 7 | מיגרציה: `?? base` + זריעה מ-`maxRid('S-')` + ייחודיות בדפוס seenR | ✅ `חנות 10` |
| 8 | ענן: shopReceiptSeq ב-META_KEYS/metaOf + `bumpCounter('shopReceiptSeq')` — מונים רק עולים | ✅ `חנות 10` |
| 9 | `addShopRedemption`: ‏paid>0 ⇒ `rid='S-'+seq` אטומי ב-setDb (דפוס addDonation); מחזיר `{ok, rid?}` — ה-UI לא מנחש | ✅ `חנות 10` |
| 10 | UI: טוסט "אישור S-N הונפק"; כפתור 🧾 אישור — downloadReceipt עם `taxReceipt:false`, בלי שדות §46, forWhat כולל "אישור תשלום — אינו קבלה לצורכי מס" | ✅ `חנות 10` |
| 11 | בדיקות: rid רק כש-paid>0 (מתנה מלאה בלי מונה), רציפות S-1/S-2, זריעה, bumpCounter לא מקטין, בידוד R-/D-, הגנת-מקור בלי taxReceipt:true | ✅ `חנות 10` (5 בדיקות) |
| 12 | `ShopComponent.validDays?` + ריפוי (לא-סופי מוסר, שלילי → 0) | ✅ `חנות 11` |
| 13 | `couponExpiry` (since+validDays, T12:00:00+isoOf) + `couponExpired` ב-needsCare עם תאריך הפקיעה ב-hint | ✅ `חנות 11` |
| 14 | UI: "ימי תוקף" בטופס (לקופון בלבד); "בתוקף עד X" בשורת הקופון (אדום כשפג); אזהרה רכה במימוש | ✅ `חנות 11` |
| 15 | בדיקות: יום הגבול בתוקף/למחרת פג, '' בלי since/validDays, פג-ומומש לא מופיע, ריפוי | ✅ `חנות 11` (4 בדיקות) |
| 16 | e2e: מלאי 3 בזרימה הקיימת ⇒ "נותרו 2" אחרי המימוש + "אישור S-1"; שלוש הסוויטות ירוקות אחרי build | ✅ `חנות 12` |
| 17 | ידע: דוח זה + CLAUDE.md (שורת המודול + המונים) + DECISIONS (10-12 + ברירות ארכיטקט) | ✅ `חנות 12` |

## הכרעות 10-12 — מימוש
10. **מלאי** — פר-רכיב, "נותרו N" בקטלוג ובכרטיס השיוך, stockOut ברשימת הטיפול (התרעת רכש, בלי כפתור מימוש); אזל ⇒ אזהרה רכה בלבד. ✅
11. **אישור תשלום** — סדרת S- רציפה ונפרדת (זריעה+ייחודיות במיגרציה, "רק עולה" בענן), מסמך להורדה עם הכיתוב "אישור תשלום — אינו קבלה לצורכי מס". ✅
12. **תוקף קופונים** — validDays פר-קופון, "בתוקף עד" חי בכרטיס, couponExpired ברשימת הטיפול; פקיעה לא חוסמת. ✅

## סטיות ופרשנויות קטנות (לא הכרעות מוצר)
1. **עדכון מבוקר של ratchet קיים:** הגנת חנות 5 אסרה כל ייבוא `receipt` ב-AssignmentsTab; סעיף 10 בפקודה מחייב במפורש downloadReceipt שם. ההגנה עודכנה: האיסור המלא נשאר על AssignmentForm/RedeemModal, והחריג ב-AssignmentsTab תחום בהגנת-מקור חדשה (בלי `taxReceipt: true`, בלי שדות §46, עם הכיתוב) ב-shop-sreceipt.test.ts.
2. **קופון שפקע** מדווח כ-couponExpired **במקום** couponPending (לא שני פריטים לאותו קופון); קופון בתוקף עם תאריך — ה-hint שלו כולל "בתוקף עד".
3. **כפתור 🧾** מוצג על המימוש האחרון של הרכיב בשורת הכרטיס (המימושים נצברים מהחדש לישן); כל מימוש שומר את ה-rid שלו בנתונים — היסטוריית האישורים מלאה ב-DB.
4. **stockOut ממוקם בסוף** רשימת הטיפול (אחרי פריטי המוטבים) — התרעת רכש, לא משימת מוטב; ואין לו כפתור פעולה ב-HomeTab.

## שאלות מוצר שנרשמו (לא הוכרעו)
- חידוש מלאי מהיר (כפתור "+N למלאי" בקטלוג) — כרגע דרך עריכת המוצר.
- ביטול/סטורנו של מימוש (והאם האישור שלו מבוטל או ממוספר מחדש) — אין כרגע מחיקת מימוש בודד.

## שערים — מצב סופי
`verify:fast` ✓ ‏(742 בדיקות, ‏+12) · build ✓ · toggle-matrix ✓ (כולל "אישור S-1 מופיע בשורת המימוש" + "מלאי 3 → נותרו 2") · demo-walkthrough 100/100 ✓ · launch-readiness 13/13 ✓ · **אפס נגיעה ב-receiptSeq/donationSeq — מוכח בבדיקת הבידוד המורחב.**
