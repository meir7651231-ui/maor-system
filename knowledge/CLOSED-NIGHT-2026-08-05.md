# סגירת צרור-הלילה (ROADMAP-100) — 5.8.2026

הלולאה שהוזמנה ("צא לדרך בלולאה") על החמישייה המומלצת מ-`ROADMAP-100-2026-08-05.md`.
שלושה סבבים · שלושה PR-ים (‏#64 · #65 · #66) · כולם מוזגו ואומתו ביט-אחר-ביט מול gh-pages.

## סבב 1 · ‏#4 דוח-שנתי-לתורם (PR #64)
- `src/lib/annualReport.ts` — מנוע טהור: שנות-תרומה, שורות-דוח (₪/$ בנפרד,
  הערת-§46 רק עם מספר-עמותה, "דוח-ריכוז זה אינו קבלה"), דוח-כלל-התורמים עם
  מעברי-עמוד (`\f`).
- כרטיס-תומך: "📄 דוח שנתי" (שנה אחרונה); מסך-תורמים: "📄 דוחות שנתיים"
  (כולם, שנה אזרחית נוכחית). דגל `supporters.annualreport`.

## סבב 2 · ‏#10 לוג-פעולות (PR #65)
- `AuditEntry` + `AUDIT_CAP=500` + `pushAudit` (טבעת) ב-domain; `Db.audit`.
- `logAudit` ב-store (who = מייל-ענן/'מקומי'; כשל לא מפיל) — שמירת/מחיקת
  משפחה ותומכ/ת, תרומה (מונח-דינמי!), תשלום, שחזור-מגיבוי.
- רוכב על meta בסנכרון (metaOf/META_KEYS/applyMetaPartial) — הענן-מנצח.
- מסך "🧾 לוג פעולות" בהגדרות — `settings.audittrail` + מנהל בלבד.
- **באג שנתפס ונחסם ב-ratchet:** ‏`?? []` בתוך סלקטור zustand ⇒ מערך-חדש-כל-
  snapshot ⇒ לולאת-רינדור React #185 (launch-readiness תפס). ברירת-מחדל בחוץ.

## סבב 3 · צרור-השרת ‏#1/#3/#9 (PR #66) — דורמנטי עד Blaze+deploy
- **#1 מייל-קבלות:** הרחבת `mail` (ה-13, roadmap); רישום-תרומה ⇒ הקבלה גם
  לתור `mailOutbox` (גידור מלא + כשל-רך); 📧 פר-קבלה בכרטיס-התומך;
  שרת `mailOutbox` — nodemailer על ‏SMTP_URL/MAIL_FROM, ‏20/דקה.
- **#3 תזכורות:** `remindersNightly` ‏05:00 IL — קשרי-תורם שהגיע-יומם +
  מסירות-היום ⇒ תורי sms/mail; יעדים באשף (`sms.adminPhone`/`mail.digestTo`).
  גבולות: אין לוח-עברי בשרת (יארצייט = צד-לקוח); ארגון-מוצפן מדולג.
- **#9 גיבוי-לילי:** `backupNightly` ‏02:30 — ‏`backups/{slug}/{date}.json`
  ב-Storage (meta+envelope+21 אוספים), טבעת 30; הצפנת-ענן ⇒ צילום מוצפן מלידה.
- **דליפת-מונח שנתפסה ב-e2e:verticals:** "תרומה" בלוג-הפעולות ⇒ termOf
  (גם רשומת-הפעולה הנשמרת) — 10/10 נקיים.

## אינווריאנטים שנשמרו (ratchet ‏night-bundle.test.ts)
- ‏BACKUP_COLLECTIONS בשרת ≡ ‏ENTITY_COLLECTIONS בלקוח (סחף-סכמה נחסם).
- הפונקציות החדשות לא נוגעות במוני-R-/D-/S- ולא רושמות תרומות (המזכירה רושמת).
- אין חישוב עברי בשרת; ארגון-מוצפן מדולג בתזכורות; הגיבוי שומר envelope.
- ‏mail = roadmap — לא נמכר באשף עד שהשרת פרוס (טקסונומיית-הכנות).

## מה נשאר לבעלים (חלון-בעלים — לא ארכיטקט)
1. ‏Blaze בקונסולה ⇒ `firebase deploy --only functions` (Runbook עודכן).
2. ‏secrets: ‏SMTP_URL + MAIL_FROM (+ הקיימים: PAY_SECRET/SMS_*/YEMOT/GOOGLE_SA).
3. להדליק Storage (ברירת-מחדל bucket) לגיבוי-הלילי.
4. אחרי deploy — אני מעביר sms/phone/sheets/mail ל-live בשורת-סטטוס.

## מהחמישייה המומלצת נותר
‏#6 פורטל-לקוח רזה (קישור-קסם בטוקן-חתום) — היחיד שטרם נבנה; דורש ליעוס
נפרד (שרת-טוקנים או חתימה סטטית). ‏#5 חשבונית-עסקית ממתין לרו"ח (גבול-תפקיד).
