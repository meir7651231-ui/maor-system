# סינון "🕐 תרומה אחרונה" — חקירה + סגירה (6.9.2026)

**בקשת-בעלים:** "תבדוק למה נעלם הסינון של תרומה אחרונה".

## 1 · מה נמצא
- הבורר "🕐 תרומה אחרונה בתקופה" (23.8) מגודר `(monthF || gaveYearF) &&` — **קיים רק אחרי בחירת שנה/חודש**.
  בלי תקופה אין אותו כלל ⇒ מבחינת המשתמש "נעלם". אומת ב-Playwright (1024/1440): הבורר מופיע רק אחרי בחירת חודש.
- **בתצוגת-גריד** (‏`db.ui.supView='grid'`, ברירת-מחדל במסך ≤760px, נשמר ומסונכרן) **אין כותרות-טבלה** ⇒ אין
  שום דרך למיין לפי "תרומה אחרונה" (או כל עמודה). מי שעבר לגריד פעם — איבד את המיון.
- הקוד לא נמחק ולא נשבר: `supLastInPeriod`, העמודה "תרומה אחרונה" בטבלה, והמצב 'last' — הכול במקומו.

## 2 · מה נבנה (additive · אפס-כסף · הלקוח-החי ביט-זהה בלי בחירה)
- **מנוע** (`supporters/lib.ts`): `LAST_BUCKETS` (30 יום · 3 · 6 · 12 חודשים · לפני יותר משנה · ללא תרומה) +
  `supLastInBucket(sp, todayIso, bucket)` — דטרמיניסטי, קבלות+היסטוריה דרך `supLast`.
- **מסך-התורמים**: בורר **"🕐 תרומה אחרונה" תמיד-גלוי** בסרגל-הסינון (לפני בוררי-התקופה); גלולה עם מונה;
  "נקה הכל" מאפס; `filtered` מכיר בו. בורר-התקופה הישן נשאר כמו-שהוא (נפרד, מגודר-בתקופה).
- **מיון בגריד**: בורר "↕ מיון" (שם/תרומות/₪/$/תרומה-אחרונה/קשר-הבא/ציון × עולה/יורד) — אותו `sort` של הטבלה;
  נעלם ברשימה (הכותרות ממיינות).
- **ratchets**: `last-donation-filter.test.ts` (6) · `e2e/last-filter-smoke.mjs` (11/11).
