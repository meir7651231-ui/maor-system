# CLOSED-SHOP5 — דוח מסירה · יומן · קופה רושמת · מיזוג פריטים · פגישות קרובות

**תאריך:** 2026-07-30 · **ענף:** `claude/what-do-you-see-bcxttj` · **מנדט:** `knowledge/BUILD-ORDER-SHOP5-2026-07-30.md` (גל A בתוכנית-האב)

## שורת סיכום
כל 4 האשכולות בוצעו (קומיטים `חנות 21·`–`חנות 24·`). בדיקות 765 → **774** (0 אדומות) · שלוש הסוויטות ירוקות כולל ההרחבות (קופה נפתחת ממולאת והמודאל שורד · הפגישה ביומן החדרים · סקשן פגישות קרובות) · **הבידוד הכספי ללא שינוי — הקופה הרושמת נשארת localStorage/sessionStorage נפרד, אפס כתיבה ל-db.**

## טבלת פריט-מול-סטטוס

| # | פריט הפקודה | סטטוס |
|---|---|---|
| 1 | קיבוע היומן: פגישת-חנות-עם-חדר מופיעה בגריד buildSlots של החדר באותו יום (דרך המקושר; יום אחר — לא) — ratchet + assert ב-e2e | ✅ `חנות 21` |
| 2 | קופה מהמימוש: CashRegister קורא `maor_cashbox_client` (דפוס amount); כפתור "💵 גבייה בקופה" ב-RedeemModal (core.cashbox + paid>0) ⇒ sessionStorage + ‎#cashbox; המודאל שורד (מוכח ב-e2e) | ✅ `חנות 21` |
| 3 | בדיקות: הדגל + paid>0, קריאת client, ratchet היומן, אפס-כתיבה-ל-db של הקופה | ✅ `חנות 21` (3 בדיקות) |
| 4 | `mergeShopItems(targetId, sourceId)` — הסבת רכיבים בכל החבילות, מלאי מתחבר, מקור נמחק, מימושים לא נגעו; חסום: חסר/עצמי/סוגים-שונים | ✅ `חנות 22` |
| 5 | UI: "⇄ מיזוג לתוך…" בפאנל הפריטים — select יעד מאותו kind, useArmed, טוסט "מוזג — הנותר M" | ✅ `חנות 22` |
| 6 | בדיקות: 3+2 עם מימוש בכל אחד ⇒ נותר 3; הסבה בשתי חבילות; חסימות בלי שינוי db | ✅ `חנות 22` (3 בדיקות) |
| 7 | `upcomingMeetings(db, today, days=2)` — פגישות פתוחות היום/מחר, מיון תאריך+שעה, שם מוטב+חדר | ✅ `חנות 23` |
| 8 | UI: סקשן "🤝 פגישות קרובות" בראש HomeTab — צ'יפ היום/מחר, שעה·מוטב·חדר, פתיחת הפגישה, ✓בוצע; ריק ⇒ מוסתר | ✅ `חנות 23` |
| 9 | בדיקות: מחר נכלל/מחרתיים לא, done מוחרג, מיון (בלי שעה — לסוף) | ✅ `חנות 23` (3 בדיקות) |
| 10 | e2e: יומן החדרים מציג "פגישת ליווי"; "פגישות קרובות" עם הפגישה של היום; שלוש הסוויטות ✓ | ✅ `חנות 24` |
| 11-12 | ידע: דוח זה + CLAUDE.md (שורת SHOP5) + DECISIONS (19-22); DoD מלא | ✅ `חנות 24` |

## הכרעות 19-22 — מימוש
19. **יומן** — הפגישות מגיעות דרך ה-OrgEvent המקושר בלבד; היומן לא קורא shopEvents (הבידוד נשמר); מקובע ב-ratchet. ✅
20. **קופה רושמת** — ממולאת (סכום+לקוח) ב-sessionStorage, נפתחת ב-#cashbox, המודאל שורד; הקופה נשארת כלי ספירה — אפס כתיבה ל-db/S- (נאכף בהגנת-מקור). ✅
21. **מיזוג פריטים** — המלאי מתחבר (סכום), מימושים לעולם לא נמחקים. ✅
22. **תזכורת פגישה** — משטח "פגישות קרובות" (היום/מחר); אין תשתית push — לא הומצאה. ✅

## סטיות ופרשנויות קטנות
1. **מיקום כפתור הקופה** — בתוך form-grid של המודאל (שדה "קבלת המזומן") ליד שדה התשלום; הפקודה לא קבעה מיקום מדויק.
2. **חסימת מיזוג בין kinds** נאכפת גם ב-store (לא רק בסינון ה-UI) — כדי שה-ratchet יתפוס גם קריאה תכנותית.
3. **✓בוצע בפגישות קרובות** עובר דרך upsertShopEvent הקיים — פגישה-עם-חדר שומרת את המקושר מסונכרן (done).

## שאלות מוצר שנרשמו (לא הוכרעו)
- קישור חשבונית-קופה למימוש (מספר חשבונית על ה-redemption) — כרגע שני העולמות נפרדים לגמרי, בכוונה (הכרעה 20).
- תצוגת פגישות קרובות גם במסך הבית הראשי — כרגע רק בעמודה (בידוד משטחים; שייך ל-CONNECT).

## שערים — מצב סופי
`verify:fast` ✓ ‏(774 בדיקות, ‏+9) · build ✓ · toggle-matrix ✓ (קופה ממולאת + מודאל שורד · יומן · פגישות קרובות) · demo-walkthrough 100/100 ✓ · launch-readiness 13/13 ✓ · **הבידוד הכספי ללא שינוי.**
