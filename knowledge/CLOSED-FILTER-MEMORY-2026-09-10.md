# זיכרון-סינון בין מסכים — סגירה (10.9.2026, ‏#490)

**בקשת-בעלים:** "שימור גם את הסינון כמו בחזרה, שלא ימחוק את הסינון".

## מה נמצא
- פתיחת-כרטיס **כבר** שמרה את הסינון — הכרטיס נפתח בתוך רכיב-הרשימה (אותו mount).
- **מעבר למסך אחר וחזרה** (לוח/בית/הגדרות) איפס הכול: המסננים היו `useState` מקומי, והרכיב יורד ועולה.

## מה נבנה
- `src/lib/filterMemory.ts` — `useRemembered(key, initial)`: useState + effect שכותב ל-Map בזיכרון-הסשן; ה-initial רק בפעם הראשונה. אותו דפוס כמו `scrollMemory` (רענון = נקי, במכוון — סינון ישן לא "נדבק" ליום המחרת).
- **תורמים** 17 (חיפוש, קטגוריה, אזור, ייעוד, דרגה, עמודות, מעקב, יעדי-קשר, הו"ק, חודש, שנת-גיוס, שנת-נתינה, מצב-תקופה, תרומה-אחרונה, פאנל-מתקדם, מיון, סגמנט — בקשת-סגמנט מהבית גוברת דרך `useEffect` חד-פעמי) · **משפחות** 10 · **חוגים** 9.
- "נקה הכל" נזכר גם הוא.
- ratchets: `filter-memory.test.ts` (5, כולל ייחודיות-מפתחות 36) · `e2e/filter-memory-smoke.mjs` (10/10).

## גם: 40 ימים במצב "פעילה" (#489)
"כל התיקונים קיימים חוץ מ-40 יום" — אצל תורם עם סגולה פעילה הכפתור הענק הוחלף בשורת-טקסט 13px. עכשיו פס ענק זהה-גודל "🕯 40 ימים — סגולה פעילה · יום X מתוך 40 · סיום dd/mm".

## לקח
- מצב-רשימה שהמשתמש בנה (סינון/מיון/גלילה) הוא נתון-עבודה — לא מאפס אותו מעבר-מסך. כל מסך-רשימה חדש: `useRemembered` + `useListScrollRestore`.
