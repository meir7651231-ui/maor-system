# ✅ CLOSED-CLOUD2 — הרשמה → אישור → עריכה חיה → "יש לו אתר"

**בנאי · 30.7.2026 · ענף:** `claude/what-do-you-see-bcxttj` · מנדט: `BUILD-ORDER-CLOUD2-2026-07-30.md` (עדיפות עסקית עליונה).

## 🔴🔴 שני צעדי הבעלים — בלעדיהם הפלטפורמה לא מאובטחת 🔴🔴

1. **לפרסם את Rules v2 עכשיו:** קונסולת Firebase ← Firestore ← Rules ← להדביק את תוכן `firestore.rules` מהריפו ← Publish.
   **⚠️ ממצא אבטחה מבדיקת-האמת: החוקים המפורסמים כיום בפרויקט החי מתירניים** — משתמש מאומת רגיל (לא ברשימה) הצליח לכתוב מסמכי platformOrgs ולקרוא אותם. גם ה-allowlist v1 שבריפו כנראה לא פורסם. פרסום v2 סוגר את זה: בקשות = רק המשתמש עצמו; ארגונים = חברים בקריאה, מיילי-על בכתיבה; אוספי-השורש = allowlist כהיום.
2. **ההרשמה ב-Auth כבר דלוקה** (נבדק — signUp מחזיר 200). אם תרצו לסגור זמנית עד פרסום v2: ‏Authentication ← Sign-in method ← Email/Password.

## מה נמסר (5 קומיטים, `ענן 1 ·`–`ענן 5 ·`)

| קומיט | תוכן |
|---|---|
| `ענן 1 ·` | שכבת נתיבים פר-ארגון: ‏`colPath`/`metaPath` טהורים; ‏`OrgConfig.cloudRoot` (רק true מפורש); ‏cloud.ts כולו דרך ה-scope (ברירת-מחדל בטוחה = שורש); ‏maor-hachesed קיבל `"cloudRoot": true` — **השינוי היחיד בקובץ**; ratchet: ‏cloudRoot מפיק את נתיבי-היום ביט-זהה לכל 18 האוספים |
| `ענן 2 ·` | קונפיג בענן + שידור חי: ‏`lib/cloudConfig.ts` (‏fetch/watch ב-onSnapshot — הלב של העריכה-בלייב); ‏`resolveOrgConfig` טהור (ענן>סטטי>ברירת-מחדל; slug מהכתובת, firebase מהסטטי); מטמון `maor_cloudcfg:{slug}` (עלייה-מהירה/offline, נפרד מדריסת-האשף); ‏loadOrgConfig משמר את ה-slug בנפילה לקונפיג-השורש (ארגון חדש בלי קובץ סטטי מקבל את ה-firebase מהשורש) |
| `ענן 3 ·` | הרשמה + Rules v2: לשוניות כניסה/הרשמה ב-LoginScreen; ‏cloudSignUp (ולידציה טהורה signUpError + ‏platformRequests/{uid}); **שער-החברות** — משתמש-ללא-חברות רואה מסך המתנה ("הבקשה נקלטה"), לא את האפליקציה ולא סנכרון; מייל-על עוקף (`SUPER_ADMIN_EMAILS` — מקום אחד); ‏firestore.rules v2 בריפו |
| `ענן 4 ·` | לוח הבקרה `#platform` (מיילי-על בלבד, דפוס #builder): בקשות ממתינות (אישור ⇒ סלאג slugify עברית→לטינית + ארגון all-off + members=[מייל המבקש] + מחיקת הבקשה; דחייה useArmed) · רשימת לקוחות + "📋 העתק קישור" · **עורך חי** — ורטיקלים/מודולים/ערכה/דגלים/מונחים, כל מתג נכתב מיד לענן; "שמור — הוקם" ⇒ provisioned |
| `ענן 5 ·` | סגירה — הסוויטות המקומיות ירוקות ללא שינוי; בדיקת-אמת מול הפרויקט החי (למטה); ידע |

**עדכון פקודה 30.7 (זרימת-שיחה) מומש:** שם איש-קשר + טלפון חובה בהרשמה; הבקשה נושאת contactName/phone; הטלפון לחיץ (tel:) בלוח הבקרה; "הבקשה נקלטה! נאשר בהקדם". מוקאפ המעצב טרם נמסר — נבנה בדפוס הקיים, המוקאפ ייושם בגל המשך.

**הכרעות ארכיטקט מומשו:** סלאג נגזר-עריך · לידה all-off (כל 8 המודולים false מפורש) · הלקוח הקיים על cloudRoot:true (נתוני-השורש לא נגעו) · מסמך בקשה נמחק באישור/דחייה · האשף הקיים לא נגוע (הפאנל משתמש ב-VERTICAL_PACKS/FEATURES/TERM_DEFS/THEME_LABELS).

## אימות מקומי (DoD)

- verify מלא ירוק — **840 בדיקות / 126 קבצים**; typecheck+lint נקיים.
- **שלוש הסוויטות ירוקות ללא שינוי** (ענן-כבוי — המסלול המקומי לא נפגע): toggle-matrix 5 פרופילים+קיצון · demo 100/100 · launch 13/13.
- הלקוח הקיים: ratchet נתיבים ביט-זהה + קונפיג עם cloudRoot:true + שער-החברות לא חל עליו (וגם לא על אתר-השורש slug 'default' — הכרעת בנאי, מונעת נעילת משתמשי-השורש הקיימים).
- אפס נגיעה בבידוד הכספי וב-DB המקומי (persist/מיגרציה — רק תוספת shopIntakes מ-SHOP6 שקדם).

## 🧪 בדיקת-האמת מול הפרויקט החי (ארגון test-demo) — צעד-צעד

הסביבה: דפדפן-הבדיקות בסביבת-הריצה נחסם ב-TLS מול ה-proxy (‏ERR_CONNECTION_RESET ל-identitytoolkit; ה-UI הציג נכון "אין חיבור לאינטרנט"), ולכן הזרימה הושלמה ב-REST — **אותם API בדיוק שה-SDK קורא**. סקריפט הדפדפן: `e2e/cloud2-truth-test.mjs`.

| צעד | פעולה | מה נצפה |
|---|---|---|
| 1 | פתיחת `?org=test-demo` (אין קובץ סטטי) | מסך הכניסה עלה עם קונפיג-השורש (firebase מהשורש, slug נשמר); לשוניות כניסה/הרשמה מוצגות ✓ |
| 2 | הרשמה (REST ‏accounts:signUp) | ‏200 — משתמש נוצר: ‏`maor.test.demo.cloud2@gmail.com` ‏(uid ‏GGPygf9XcTQDiBJ1F9NzZDWXHqu1) ✓ — ההרשמה ב-Auth דלוקה |
| 3 | כתיבת `platformRequests/{uid}` בטוקן המשתמש | ✓ נכתב — **וגם חשף שהחוקים החיים מתירניים** (ראו הצעד למעלה) |
| 4 | אישור (סימולציה): יצירת `platformOrgs/test-demo` — קונפיג all-off, ‏members=[המייל], ‏provisioned:false | ✓ נכתב |
| 5 | קריאה חוזרת כמו הלקוח | ✓ "עמותת בדיקה" · provisioned=false |
| 6 | **מתג חי**: ‏config.modules.families ‏false→true (updateMask) וקריאה חוזרת | ✓ הערך התעדכן — זה בדיוק המסמך ש-onSnapshot של הלקוח מאזין לו (העריכה-בלייב) |
| 7 | מחיקת הבקשה (כמו באישור אמיתי) | ✓ נמחקה |
| 8 | נתוני-השורש | **לא נגעו** — כל הפעולות ב-platformOrgs/platformRequests בלבד ✓ |

**מה לא כוסה כאן (מגבלות סביבה, לבדיקת הבעלים בדפדפן אמיתי):** שני-דפדפנים במקביל עם onSnapshot חי (הקוד — `watchOrgCloudConfig` — נבדק ביחידה ובהגנות-מקור); אין emulator בגל הזה.

**שאריות הבדיקה בפרויקט החי (לניקוי בקונסולה כשנוח):** משתמש Auth ‏maor.test.demo.cloud2@gmail.com (מחיקה דורשת קונסולה/Admin SDK — המגבלה מהמנדט) + מסמך `platformOrgs/test-demo`. אחרי פרסום v2 אין לחשבון הזה שום גישה (אינו חבר בשום ארגון).

## הכרעות בנאי (ניתנות לדריסה)

1. **נתיבי הפלטפורמה:** ‏"platform/orgs/{slug}" מהפקודה אינו נתיב-מסמך חוקי ב-Firestore (מספר מקטעים אי-זוגי) ⇒ מומש כ-`platformOrgs/{slug}` ו-`platformRequests/{uid}` — אותה סמנטיקה, נתיבים חוקיים.
2. **אתר-השורש (slug 'default') פטור משער-החברות** — אחרת משתמשי הלקוח הקיים שנכנסים בלי ‎?org= היו ננעלים על מסך ההמתנה.
3. ‏members נשמרים באותיות קטנות (האישור כותב lowercase) — ההשוואה ב-Rules פשוטה.
4. ‏SUPER_ADMIN_EMAILS בקוד = ‏meir7651231@gmail.com (זהה ל-Rules) — הוספת מייל-על = שני מקומות (config.ts + rules), מתועד.

## שאלות פתוחות

- ממצא האבטחה (חוקים חיים מתירניים) — לא חוסם את ההמשך, אבל **פרסום v2 דחוף**.
- מדיניות דאטה ללקוח שנדחה/נמחק (שאלה 4 מהעיצוב) — טרם הוכרעה; כרגע דחייה מוחקת רק את הבקשה.
