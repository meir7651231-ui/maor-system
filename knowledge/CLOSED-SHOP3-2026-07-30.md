# CLOSED-SHOP3 — דוח מסירה · חידוש מלאי מהיר + ביטול מימוש עם סימון

**תאריך:** 2026-07-30 · **ענף:** `claude/what-do-you-see-bcxttj` · **מנדט:** `knowledge/BUILD-ORDER-SHOP3-2026-07-30.md`

## שורת סיכום
כל 3 האשכולות בוצעו (קומיטים `חנות 13·`–`חנות 15·`). בדיקות 742 → **750** (0 אדומות) · שלוש הסוויטות ירוקות כולל ההרחבה (חידוש מלאי ‎+2 ⇒ "נותרו 4" · ביטול ⇒ "מבוטל" והסכומים יורדים) · **ratchet רציפות ה-S- ירוק: ביטול לא מוחק רשומה, לא ממחזר מספר, והבא בסדרה ממשיך.**

## טבלת פריט-מול-סטטוס

| # | פריט הפקודה | סטטוס |
|---|---|---|
| 1 | כפתור + על צ'יפ המלאי (title="חידוש מלאי") ⇒ StockModal: "נותרו N היום", כמות חיובית, `stock=(stock??0)+n`, טוסט "המלאי עודכן — נותרו M" | ✅ `חנות 13` |
| 2 | רכיב בלי מעקב — אותו מודאל עם "אין מעקב — הזנת כמות תפעיל מעקב" | ✅ `חנות 13` |
| 3 | בדיקות: הגנת-מקור כפתור + מסלול הפעלת-מעקב + שער קלט לא-חיובי | ✅ `חנות 13` (3 בדיקות) |
| 4 | `voidedAt?`/`voidReason?` + ריפוי מיגרציה (voidedAt לא-מחרוזת מוסר) | ✅ `חנות 14` |
| 5 | **helper יחיד `liveRedemptions`** — givenValue/collectedPaid/subsidyTotal/componentRemaining/assignmentRedeemed כולם דרכו; אפס סינונים מפוזרים | ✅ `חנות 14` |
| 6 | `voidShopRedemption(assignmentId, redemptionId, reason)` — חסום על מבוטל-כבר; voidedAt=isoToday; **לא נוגע במונים, ה-rid נשאר** | ✅ `חנות 14` |
| 7 | UI: 🚫 ביטול בדו-קליק (useArmed) + prompt לסיבה; שורה מבוטלת בקו-חוצה + צ'יפ "🚫 מבוטל ב-X" (title=הסיבה) + הסתרת 🧾; ה-rid מוצג | ✅ `חנות 14` |
| 8 | בדיקות: החרגה מסכומים+החזרת מלאי · מתנת-חג חוזרת ל-holidayDue · ביטול-כפול חסום · **רציפות: rid נשאר, המונה לא זז, הבא S-2** · ריפוי · הגנת-מקור | ✅ `חנות 14` (5 בדיקות) |
| 9 | e2e: חידוש ‎+2 ⇒ "נותרו 4"; ביטול ⇒ "מבוטל" + הסכומים ל-0; שלוש הסוויטות אחרי build | ✅ `חנות 15` |
| 10 | ידע: דוח זה + CLAUDE.md (שורת SHOP3) + DECISIONS (הכרעות 13-14 + ברירות) | ✅ `חנות 15` |

## הכרעות 13-14 — מימוש
13. **חידוש מלאי מהיר** — מהקטלוג, בלי לפתוח את עורך המוצר; מפעיל מעקב לרכיב שלא היה במעקב. ✅
14. **ביטול עם סימון** — הרשומה לעולם לא נמחקת; המלאי והסכומים מתוקנים אוטומטית (דרך liveRedemptions); מספר ה-S- נשאר בסדרה עם חותמת ביטול — הסדרה רציפה ולא ממחזרת. ✅

## סטיות ופרשנויות קטנות (לא הכרעות מוצר)
1. **היסטוריית מימושים מלאה בכרטיס:** כדי ששורה מבוטלת תישאר גלויה, שורת הרכיב בכרטיס השיוך מציגה כעת את **כל** המימושים של הרכיב (חדש→ישן), לא רק את האחרון — כל שורה עם 🧾/🚫 משלה. שדרוג תצוגה נלווה מתחייב מהכרעה 14.
2. **צ'יפ מלאי גם לרכיב בלי מעקב** ("ללא מעקב" + כפתור +) — נדרש כדי שסעיף 2 (הפעלת מעקב מהקטלוג) יהיה נגיש.
3. **סיבת ביטול** נאספת ב-window.prompt (רשות, נשמרת גם ריקה) — כמפורט בפקודה; מוצגת כ-title על צ'יפ הביטול.

## שאלות מוצר שנרשמו (לא הוכרעו)
- שחזור (un-void) של מימוש שבוטל בטעות — כרגע אין; מימוש חדש הוא המסלול.
- הצגת מבוטלים בסכום נפרד ("בוטלו השנה: X ₪") בדוח/ראווה — לא נדרש בפקודה.

## שערים — מצב סופי
`verify:fast` ✓ ‏(750 בדיקות, ‏+8) · build ✓ · toggle-matrix ✓ (נותרו 2 → חידוש → נותרו 4 → ביטול → "מבוטל" + סכומים 0) · demo-walkthrough 100/100 ✓ · launch-readiness 13/13 ✓ · **ביטול לא מוחק רשומה ולא ממחזר S- — מוכח ב-ratchet הרציפות.**
