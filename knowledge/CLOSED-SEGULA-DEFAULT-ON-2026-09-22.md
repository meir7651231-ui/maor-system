# סגירה · 🕯 40 יום דלוק ברירת-מחדל לכל עמותה — 22.9.2026 (PR ‏#501)

## הבקשה (בעלים, שלוש פעמים)
"זה עדיין לא מופיע — 40 יום יהיה כפתור דלוק ברירת-מחדל." הלקוח שעל פלטפורמת-הענן לא ראה את כפתור ה-40 יום, למרות התיקונים הקודמים (#494 הודעת-דגל, #496 בורר-חזרה, #498 שומר-מסך).

## השורש (סוף-סוף מדויק)
ארגון-פלטפורמה נולד `all-off` (`allOffConfig` — `features:{}` אבל האשף שהקים אותו כתב `supporters.segula:false` לקונפיג-הענן). לפי חוזה-הדגלים "מפתח חסר=דלוק, רק false מכבה" — ה-false המפורש הסתיר את הכפתור. הלקוח-החי (root, `default`) מעולם לא נפגע כי אצלו הדגל חסר=דלוק.

## ההכרעה + התיקון
**הכרעת-בעלים: 40 יום = דלוק ברירת-מחדל בכל עמותה, לא ניתן-לכיבוי-בשקט דרך הדגל.**
- `SupporterDetail.tsx`: `const segulaOn = featureOn(config, 'core.taxreceipt')` — הנראות נגזרת מ"זו עמותה" ולא מ-`supporters.segula`. דגל `false` כבר לא מסתיר. מוסתר רק בוורטיקל מסחרי (`!core.taxreceipt` — שם "העין"=פרויקטים, והסגולה חסרת-משמעות; עקבי עם AyinCard/SupportersView).
- הוסרה הודעת-הדגל-הכבוי (#494) — כבר לא רלוונטית.
- `SettingsView` אבחון-דגלים: `40 יום נראה: ON/OFF (ברירת-מחדל לעמותה)` + הדגל הגולמי לאבחון.
- `features.ts`: תיאור הדגל עודכן ("דלוק-ברירת-מחדל; הדגל אינו מסתיר").

## אינווריאנט
הלקוח-החי (עמותה, root): הכפתור כבר הופיע (חסר=דלוק) ⇒ **ביט-זהה**. השינוי משפיע רק על ארגונים שבהם הדגל נכתב `false` (בעיקר לקוחות-ענן שהוקמו דרך האשף all-off).

## ratchets + שערים
- `src/lib/__tests__/teachers-csv-segula-diag.test.ts` — הגנות-מקור עודכנו לכוונה (segulaOn=core.taxreceipt · אין `{!segulaOn && (` · אין הודעת-כבוי · אבחון "40 יום נראה").
- `e2e/teachers-csv-segula-diag-smoke.mjs` — המקרה (ג): דגל `false` בעמותה ⇒ הכפתור **עדיין מוצג**, בלי הודעת-כבוי; אבחון "40 יום נראה: ON למרות raw=false". 10/10.
- `e2e/segula-smoke.mjs` 16/16 · `verify:fast` 2768.

## פריסה
main ‏77e95a9 → github.io (deploy.yml) + orbit-il (trigger ב-push ל-README). אימות: `40 יום נראה` בצ׳אנק SettingsView המוגש + היעלמות `כבוי בהגדרות-הארגון` מצ׳אנק SupporterDetail.
