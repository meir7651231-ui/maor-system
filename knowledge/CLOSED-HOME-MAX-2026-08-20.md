# CLOSED · לולאת "בית למקסימום" (19–20.8.2026, PR ‏#232 / ‏#234 / ‏#235)

**המנדט (בעלים):** "תעבור פיצ'ר-פיצ'ר במסך הבית, תשפר למקסימום, תחווט מקצה-לקצה, תבדוק תקינות, תשפר ויזואלית ותקליל — בלי להוריד יכולות, בלולאה ללא עצירות."

**השיטה:** ביקורת-60-סוכנים (ultracode workflow — 8 סורקים ⇒ 103 ממצאים ⇒ אימות אדוורסרי של 52 ⇒ **14 מאומתים**) במקביל ליישום ישיר. שלושה סבבים, כל אחד: אשכולות ⇒ verify:fast ⇒ commit ⇒ שלוש סוויטות-דפדפן ⇒ PR ⇒ squash-merge ⇒ פרודקשן. בדיקות: 1621 ⇒ **1787**.

## סבב א׳ (#232) — חיווט-עומק + נכונות
- **עומק:** supnext/יעדי-קשר ⇒ כרטיס-התומך (`openSupporterCard`; ‏`AttentionNav` קיבל `{kind:'supporter'}`) · אירוע-דחוף-מקושר ⇒ כרטיס-המשפחה · 📞/💬 בשורות-יעדי-קשר · "➕ הוספת משפחה" ⇒ `openFamilyForm` (הטופס עצמו) · אריחי-דרגות לחיצים בשני ווידג'טי-האמינות (`openFamiliesByTier`).
- **נכונות:** ‏**vdb לווידג'טים** (עובדת מוגבלת-ייעוד לא רואה תורמים זרים בשום ווידג'ט — `ctx.db = vdb`) · **"לכולל"** (הכרעת 9.8): donIls/donUsd + צ'יפ-החודש = קבלות+hist דרך supIls/supUsd · תיקון-קרוסלה (idx חסום, נקודה-לכל-פריט, בלי ‎%8‎) · `suggest` חזר ל-3 תבניות · FULL_LAYOUTS לכל 4 הערכות · `activeTheme` אחיד · שמירת-לוח לא משמיטה מוסתרים-זמנית · דופק-דקה (ברכה/חצות) · גידור-מודולים במקור (דחוף/ממתינות/ספח/מדד/תקציר).

## סבב ב׳ (#234) — אסימוני-ערכה + הרחבות
- **דין-נדחה מלא ב-holidayOf:** צום גדליה נדחה (שבת⇒ראשון; אומת 5-6.10.2024) · תענית אסתר מוקדמת (שבת⇒חמישי י"א; אומת 21-24.3.2024). משלים י"ז-בתמוז/ט'-באב.
- **ערכתיות:** צבעים קשיחים ⇒ ‏var(--red/--green/--orange/--accent) (נקודות/עמודות/מגמה/איפוס) · ריחופים (חוגג/מפגש/אריח) · `hm-row static` ללוח-העברי · יעדי-מגע ≥24px · clamp לכותרות-hero במובייל.
- **מונים:** צ'יפ 🤝 פגישות-היום (careCounts.shopMeetings — מונה-עם-קפיצה) · **מונה-חלוקה סופר ימים-פתוחים שחלפו** (`pendingDeliveriesToday`: ‏`date<=today && !closed` — מסירה לא נעלמת בחצות; closed=ארכיון) · תווית-קבוצה + gi/groups ב-TodaySession · הו"ק "שעבר יום-החיוב".

## סבב ג׳ (#235) — סגירת 14 ממצאי-הביקורת
- 🔴 **HIGH: מסלול-התבנית עקף את FULL_LAYOUTS** — ב-`home.board:false` ארבעת ווידג'טי-האנליטיקה לא עלו כלל (ה-ratchet הישן בדק מחרוזות בלבד ועבר). התיקון: `tplIds`/`extras` — עודפי-הפריסה מרונדרים אחרי התבנית בקיבוץ-חצאים; אפס-עודפים ⇒ ביט-זהה.
- "נוכחות ✓" ⇒ `openCourseAttendance` (store: ‏courseAttnReq, גלילה לטבלת-השיבוצים ב-CourseDetail) — עומק אמיתי מעבר לקליק-השורה.
- רשומות-פר-קבוצה = סמנטיקת-היומן (`!e.group || e.group === label`).
- גידור-מנוע ב-shop8 (חג⇒חנות · חידוש⇒חוגים) + גידור-מקטעים בפאנל-היום + קרוסלה דורשת מקור-תוכן.
- `home.care:false + home.crosscare:true` ⇒ הפאנל מארח-צ'יפים-בלבד (הדגל לא מת בשקט).
- **מפתחות-מגורסנים ל"טופל"**: ‏`supnext:{id}:{nextDate}` · `debt:{id}:{dueDate}` · `punch:{id}:{used}` — סימון ישן פג כשהמצב מתקדם. ‏'sug:' פטורות מגיזום-30-יום.
- termOf ל"מדד אמינות" (3 משטחים + labelTerm) · מד-אמינות RTL (ציון-גבוה על הזהב) + ציר-לפי-ערך + סל 950-1000.

## לקחים
1. **ratchet-מחרוזות אינו ratchet-התנהגות** — ערובת-הפריסה-המלאה "עברה" בעוד ההתנהגות שבורה; ממצא ה-HIGH נתפס רק בביקורת. כשמדובר במסלול-רינדור — לבדוק גם את זרימת-הענפים (tplIds/extras), לא רק הימצאות-מחרוזת.
2. **wLabel שהחליף במקום להחליף-בתוך** קטע שמות בעורך ("תפוסת החוגים"⇒"חוגים") — תבנית substitute-in-label.
3. **מפתח-ביטול חייב לגרסן את המצב** — אחרת "טופל" מסתיר לנצח מצבים חדשים.
4. הצ'יפים החוצי-עמודות תלויים בפאנל-מארח — כשמפצלים דגלים (care/crosscare) לוודא שלכל דגל יש משטח.

**סטטוס:** 14/14 ממצאים סגורים · 1787 בדיקות · שלוש סוויטות-דפדפן ירוקות בכל סבב · שלושה squash-merges לפרודקשן.
