# נחיל ב׳ · ביקורת-עומק סבב ב׳ — סגירה (3.9.2026)

**בקשת-הבעלים:** "תריץ נחיל עמוק יותר" (אחרי `CLOSED-DEEP-AUDIT-2026-09-02.md`). הופעל נחיל-9×9 שטוח
(`/swarm`, ‏PLAYBOOK של בנייה-חכמה): **10 מבקרים** (עדשה לכל אחד, לבושי-ממד 6+9) → **4 מאמתים
אדוורסריים** (ממד 4+6) → **5 מתקנים** על קבוצות-קבצים דיסיונקטיות (ממד 3) → אימות-בייטים →
שער מלא → מוטציה → מפקח. חדשים-בלבד (לא מה שנסגר בסבב א׳).

## מספרים
| שלב | תוצאה |
|---|---|
| מבקרים | 10/10 חזרו · ~75 ממצאים · 310 שורות |
| מאמתים | ~45 מאושרים/מתוקנים · ~10 נפסלו · ~15 בקלוג/הכרעת-בעלים |
| מתקנים | 5 · 54 קבצים · אפס חפיפה (diff ≡ איחוד-החלוקה) |
| שער | typecheck ✓ · lint ✓ · **2646/2646** יחידה (372 קבצים) · build 157.2KB gzip · דפדפן 16 סוויטות · functions |
| מוטציה | A1 (שער-flush) ו-MN-1 (שער-סכום הו״ק) ⇒ אדום ⇒ שוחזר ⇒ ירוק |

## מה תוקן (לפי חומרה-מאומתת)

### HIGH
- **A1 · דריסת-DB ריק ביציאה מוקדמת** (`useApp.ts` flush): pagehide/visibilitychange לפני סיום `init()` כתב `emptyDb()` על `maor_db` (ואצל `?org=` — על מפתח-השורש). שער `!s.ready || s.needDecrypt`. הוכח בבדיקה.
- **P1 · הלוח העברי קופא** (`calLib.ts`/`hebrew.ts`): מטמון-hpOf 3000 עם `clear()` ⇒ 2.4s לגריד ב-3000 משפחות. תקרה 30k + evict-oldest ⇒ 10ms.

### MED
- **F1 · סמכות-מנהל אפקטיבית** (`config.ts` `isAdminAuthority`): ארגון-פלטפורמה נולד בלי `adminEmails` ⇒ `isAdminUser` החזיר true לכל עובדת ⇒ פעולות-מחיקה-המוניות + סעיפי-מנהל (AI-key, לוג-פעולות, ייבוא-תורמים, gcontacts, ערכה) נפתחו. אומת: שני קונפיגי-החי מגדירים adminEmails ⇒ **ביט-זהה** לבעלים.
- **F4 · איפוס/שחזור בענן** — רק סמכות-מנהל (UI+store), אחרת הודעה. אופליין/שורש ללא-שינוי.
- **F2 · הענקה-פר-עובד מעל תקרת-הארגון** (`platform/lib.ts`): `false` ארגוני גובר.
- **F3/N5 · מורה מגיע להגדרות** דרך ⌘K/DayGate — צ׳וק ב-App + סינון בפלטה.
- **N3 · אזור-PIN 'wizard' לא נאכף מעולם** — נאכף על אשף-ההרכבה.
- **N1/N2 · מבוי-סתום** במסכי-אין-הרשאה (כפתור-חזרה) · Escape סוגר רק את המודאל העליון (מקודם סגר הכול ומחק טופס).
- **MN-1 · הו״ק המוני מנפיק D- על סכום 0/NaN/שלילי** — שער לפני צריכת-המונה.
- **MN-2 · מיזוג-כפולים מאבד חיובים-מתוכננים** — איחוד לפי id.
- **DS-1 · ייבוא בלי תגית-מכשיר** (10 אתרים ⇒ `makeId`) — התנגשות-מזהים בין מכשירים.
- **EC-1/EC-2 · migrate** — תרומה בלי `date` ⇒ '' (הקריסה קיר/קוקפיט); איבר-null לא מפיל את הטעינה.
- **S1 · ניקוי-ארגון-שנמחק** — `staleTab` מונע כתיבה-חוזרת מהזיכרון.
- **A4/A2 · flushPush** — backoff אקספוננציאלי, toast פעם-אחת, `invalid-argument` פטאלי; נקודת-'pending' תקועה.
- **P4 · תמונות > 1MB** — תקציב 600k לתומך (`canAddPhoto(cur, nextLen)`); ⚠️ הכרעת-מוצר: 5×460k לא נכנס ל-Firestore.
- **A5 · outbox בשרת** — claim טרנזקציוני `sending` + שחרור-תקוע 10 דק׳ (כפל-SMS/מייל בריצות חופפות). דורש פריסת-Functions.
- **A6 · דאבל-טאפ SMS/מייל-קבלה** — smsBusy/mailRcptBusy.
- **TP-1/2/3 · דליפות-מונח בהורדות/טוסטים** (CSV דשבורד-חוגים, CSV ימי-חלוקה, טוסט-ייבוא) + ~12 MED (דשבורד, כרטיס-חוג, תשלומים-נכנסים, קוקפיט, מנהל-ארגון, שער-הצטרפות — **בלי לגעת בפרוטוקול-הצ'אט**, דוח-שנתי).
- **A-2/A-3/A-4 · נגישות** — שורות-חוג במקלדת · טלפון/מייל בכרטיס-משפחה `dir=ltr` · יעד-מגע 40px לקישור-חיוג (מגע בלבד).

### LOW (נסגרו)
MN-3/MN-4 עיגול-אגורות בקבלה/CSV · MN-5 זיכוי≠חיוב-הו״ק · P2 חיפוש-משפחות (planQuery, 641ms⇒~60ms) · P3 saveDb בלי parse-מלא · P6 הצעות-חיפוש ממוזכרות · S2 דגלי-PIN פר-ארגון · A7/A8/A10/N6 · F5 gcontacts מנהל-בלבד · A-5/A-7/A-8 aria · DS-2 אזהרת-ארגון-שונה בשחזור.

## נפסל (false-positive, מתועד)
N4 ‎#dedup‎ (פעולת-משפחות, לא הגדרות) · DS-3 dedup-משפחות (מכוון, ratchet) · EC-3 OneFlow.phone (תמיד string) · TP-19 'No-Show' (תג-לגאסי) · TP-16/17 סחף-fallback (ה-fallback = הטקסט-החי) · S4 override-שורש (e2e-verticals נשען עליו) · P5 SupportersView (12ms).

## בקלוג / הכרעת-בעלים (לא נשמט)
1. **A-1 ניגודיות** `--ink-faint` נכשלת AA בכל ערכות-האור (3.15:1 בחי). תיקון-טוקן מוכן (or-rishon `#94886f→#6f654f`) — **גלוי ללקוח-החי** ⇒ הכרעה.
2. **S3** איפוס-מלא לא מנקה קופה-רושמת/טיימר/מפת-גוף במכשיר — "האם איפוס מוחק היסטוריית-קופה?"
3. **P4** מספרי-תקציב-תמונות (5×460k מול 1MB).
4. **A3** ‎onRemote‎ בחלון-cloudReplaceNow — נשדרג מ"תיאורטי" ל"נגיש, נדיר"; דורש replay עם delLog — חלון ליבת-סנכרון.
5. **A9** נעילת-ריצה ל-gcontactsSync · **TP-13** askMaor במונחים · **A-6** גדלי-✕/chip · ~40 כותרות-LOW (רשימה ב-`_confirmed-text-a11y.md` §C).
6. פריסת-Functions (A5, F5) — `deploy-fn/*`.

## לקחים
- **`?raw` ל-CSS ריק תחת vitest** — readFileSync (כבר מתועד ב-shell-align; חזר).
- **מתקן בלי Write** לא יכול ליצור קבצי-בדיקה — לתת לו Write או שהאורקסטרייטור כותב.
- **ratchet פר-נוסח נשבר בתיקון-נכון** (3 פינים) — לנעול כוונה עם חלון-רחב.
- **מוטציה חובה**: שני ratchets אדומים-אז-ירוקים.

## ✅ סגירה (3.9.2026 ~03:30 UTC)
- אחרי אתחול-קונטיינר: build נקי → `full-audit` לבד **exit 0** (ממצאי OFF-VIEWPORT במובייל = טבלאות ב-`overflow-x:auto`, מידע-בלבד, מתועד ב-`CLOSED-E2E-AUDIT-2026-09-01 §full-audit`) → `demo-walkthrough` לבד **exit 0**. ⇒ **16/16 סוויטות ירוקות**.
- סיבת-השורש לשני ה"כשלים" בסוללה: `EADDRINUSE :4192` — המפקח הריץ **במקביל** הרצה-חוזרת של אותן סוויטות על אותו פורט. **לקח:** סוויטות-דפדפן רצות **בטור בלבד**; מפקח לא מריץ e2e בזמן שהסוללה רצה.
- CI על PR ‏#475: verify · rules · audit ירוקים. ממוזג (squash) — ראה CLAUDE.md.
