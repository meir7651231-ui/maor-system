# 🗺️ מפת האקוסיסטם — maor ↔ בנייה חכמה (מאומת-קוד, 30.7.2026)

> נכתב ע"י ארכיטקט maor אחרי מיפוי מלא של ריפו buildsmart (23 ענפים, 2 סוכני חקירה + אימות ידני).
> **חוק הידע (מיובא מ-nice-volta):** לפני שמסמנים משהו "פתוח"/"קיים" — מאמתים בקוד. אף טענה בלי ראיה.

## 1. שני קווים בריפו buildsmart — אל תתבלבל ביניהם שוב

| קו | ענפים | מצב |
|---|---|---|
| **הקו החי** | קוד: `claude/whats-happening-LyY9G` · ידע: `claude/nice-volta-BSbVm` | v7.01 · **פרודקשן חי ב-buildsmart-il.com** · deploy אוטומטי · 1,867 מוצרים (v2: 2,656) · 694 קובצי טסט · Backend Firebase שלם (S0-S9, ‏949 שורות rules, ‏3,373 שורות Functions, R2) — נותר ops בלבד |
| **קו המוזיאון** | `main`, ‏`compassionate-cray`, ‏`legacy-prototype` ועוד | ‏main מפגר ב-**1,220 קומיטים** אחרי הקו החי. ה-CLAUDE.md שבו מתאר עולם שנעלם (dials, INSP, ‏10 טסטים) |

ענפים שנבלעו בקו החי: kind-dijkstra (אנדרואיד APK), monster-build (מקלדת), fleet/worker-board, word-finder, agent-network. ‏maor **נולד** בתיקיית `maor/` בריפו הזה (ענפי claude/maor-system + phone-connection = הרחם ההיסטורי; הכול הועבר).

## 2. מודל העבודה של הקו החי (ממשל)

- **LAW #0:** אין עבודה בלי הפעלת נחיל 9×9 (9 תפקידים × 9 עדשות, `orchestrator/lenses/registry.txt`), מופעל ב-`/swarm`.
- **טרנק אחד** + merge-back כל ≤10 קומיטים · ff-only · אסור ענפים ארוכים.
- **פרוטוקול 123 שערים** (88 פעילים ב-pre-commit 937 שורות) + central-verify (analyze+test+build+conformance) + grep-verify ("בייטים, לא פרוזה") + mutation חובה.
- **הפרדת ידע/קוד:** ספרן כותב רק ל-nice-volta; הצי בונה רק ב-whats-happening; **המשתמש הוא הגשר**.
- push רק על מילה מפורשת ("תדחוף").
- מקור האמת לעבודה פתוחה: `knowledge/VERIFIED-OPEN-WORK-2026-07-29.md`.

## 3. העבודה הפתוחה האמיתית בבנייה חכמה (מאומת 29.7)

1. **🥇 שילוב-מאור — ‏~40% נותר (עיקר העבודה):** חיבור workflow_engine (kernel קיים, אפס צרכנים) · הארת JourneyTimeline (בנוי, כבוי) · מספור-מסמכים-רץ (receiptSeq) · יומן-אשראי+tiers · לוח-משאבים+חסימת-חג · מכסה-נגרעת · חזרתיות+דאבל-בוקינג · דוח-הרחבה-יומי · הקשחות C3/C4/C5.
2. **🥈 שער אנטי-כפילות מערכתי** (דרישת בעלים) — הגייט האוטומטי לא מומש.
3. **🥉 fake-data** — אתר בודד (store pull-to-refresh no-op).
4. **Ops השקה:** ‏USE_FIREBASE_BACKEND=true · ‏Blaze · ‏App Check · חנויות אפל/גוגל · דומיין עברי · פוליש P-1/P-5.

## 4. האסטרטגיה המאחדת — "המערכת הענקית"

`MASTER-giant-system-order.md` (27.7): מנוע ענק אחד כ-superset מקסימלי; **חברה = גולמי-נקי + קונפיג + דאטה**; יעד 30 אפליקציות white-label. מודל-האב: מאור (MaorClean↔MaorHachesed). זהה לחזון "פלטפורמה אחת — אלפי עמותות" של אשף ההרכבה במאור. **שני הריפואים הם אותו עסק בשני ורטיקלים.**

עקרון ההעברה (MAOR-REUSE-MAP): **כורים את הדפוס, לא את הפיצ׳ר** ("חדר ≈ רכב"); לוגיקה עוברת, קוד לא (מימוש מחדש ב-Dart); רק גימטריה נעולת-תחום.

## 5. השלכות מחייבות על משימת maor (הוראות ארכיטקט)

1. **כל מנוע חדש במאור = נכס כפול.** כל פקודת בנייה שלי תדרוש: לוגיקה בקבצים טהורים ובדוקים (`lib/*.ts` בלי DOM/store) — כי משם היא עוברת נקי ל-Dart. מנועי P2 (timer/cashbox/bodymap/doncal/dedup) כבר ממופים כ-additive עתידי לבנייה חכמה.
2. **מספור-מסמכים, יומן-cred, מיזוג-ענן** — הדפוסים שמאור כבר עשה נכון הם בדיוק מה שחסר בבנייה חכמה (#13, ‏#4b, ‏C5). לשמור על טהרתם.
3. **שערי maor** יובאו מהקו החי (tiered + integrity + build-at-push, לקח #72) — לשדרג בהמשך לפי דפוסי central-verify (conformance/required-tests) כשהצורך יבשיל.
4. **אזהרת תיעוד:** ה-CLAUDE.md בענף הארכיטקט של buildsmart (וגם בקו החי עצמו, שורות ה-Preact) מתאר עולם ישן. אין לתקן משם — התיעוד בסמכות הספרן/הצי.

## 6. ממצאים להעברה לבעלים (מהחקירה — לא באחריותי לתקן)

- **כפילויות מספרי-שער** ב-pre-commit החי: שערים 32, 42, 53, 102, 103, 113 מופיעים יותר מפעם אחת — ה-GATE_REGISTRY עצמו מורה לבדוק בדיוק את זה אחרי rebase.
- **ADRs סותרים:** ‏adr/001-no-window ו-002-dial-pattern עדיין "Accepted" בעוד ה-dial הוצא (manager-dashboard-MAP קובע deprecated) + PARITY/ROADMAP/CONVENTIONS עוד מצטטים R2.
- **קוד יתום:** `lib/widgets/dial.dart` קיים ואינו מיובא ע"י אף קובץ lib.
- ‏CLAUDE.md של הקו החי: שורות 45-133 עדיין מתארות את עולם ה-Preact (מטעה סוכן חדש).
