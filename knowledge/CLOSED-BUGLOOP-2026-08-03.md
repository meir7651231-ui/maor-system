# סגירת לולאת-הבאגים — ANALYSIS §5 (2026-08-03)

סשן ארכיטקט אוטונומי ("תעבוד בלולאה עד הסוף") שסגר את מרבית באגי `ANALYSIS-2026-07-29.md`
לפי הכרעות-הבעלים המפורשות. כל פער = commit נפרד עם ratchet, פרוס ל-main ואומת
ביט-אחר-ביט מול gh-pages (hash נגזר-תוכן).

## נפרסו ואומתו (PR #29–#33)

| # | הפער | התיקון | Ratchet |
|---|------|--------|---------|
| 6 | נוכחות ללא אידמפוטנטיות (לחיצה כפולה = כפל-ניקוב) | `Enrollment.presents[]` + `setPresent` החלפה אידמפוטנטית; `used` נשאר מונה-הכסף | set-present |
| 10 | "{used} נוכחויות מתחילת החודש" בלי איפוס | `presentsInMonth` — סופר לפי חודש נוכחי; ההיסטוריה ב-`used` | set-present |
| 8 | תלמידה שסיימה נעלמת מדוח-נוכחות היסטורי | `Enrollment.endedAt?`; `courseDaily` מחריג ended רק ממפגשים מ-endedAt ואילך | courseDaily |
| 7 | זכאות-השלמה לפי שעון 48ש׳ בלבד | `Absence.justified?` + `makeupEligibility` טהור (מוצדק⇒זכאי תמיד; מוקדם נשמר) | makeup-eligibility |
| 9 | הסרת מפגש הזיזה מספור "קבוצה N" | `groupRemapOnRemoval` טהור; `removeSession` ממפה שיבוצים | group-remap |
| 5 | התנגשות-מזהים בין שני מכשירים לא-מקוונים | `ids.ts` — `makeId` + `deviceTag` (base36 פר-מכשיר) ב-`nextId` | ids |
| 3 | last-writer-wins שקט בין טאבים | מאזין `storage` → אימוץ כתיבה חדשה יותר (בלי עריכה-מקומית-ממתינה) | multitab-guard |
| 5.5a | דדופ rid כפול תלוי-סדר-מערך | `planRidRenumber` — המוקדם-בתאריך שומר על מספרו; דטרמיניסטי | rid-dedup |
| 18 | אין strict mode | `strict:true` בשני ה-tsconfig — הקוד כבר null-safe, 0 שגיאות | (typecheck) |
| 22 | assertions ריקים ב-e2e (errors===0 תמיד) | אימות-DB אמיתי (R-/D-); חשף+תיקן שלב-תרומה שלא נשמר בדמו | (demo 100/100) |

**כבר תוקנו בסבבים קודמים (אומת):** #2 (ולידציית שמות-חודשים ICU), #4 (אזהרת reset-ענן),
#12 (ציר-שנה-דו-ספרתי דינמי), #13 (תקרת-ימים ב-customExport), #15 (מיזוג-ייבוא ללא-טלפון),
#16 (שרשור תת-דגלים), #17 (adminEmails בקונפיג הלקוח), #19 (fonts.css מת), 5.3 (נעילה פר-ארגון),
USD rate עריך, demo לפי תאריכים-קרובים.

## ממתין להכרעת-בעלים / רו"ח (לא נוגעים בעיוור — גבול-תפקיד)

- **#14 — אכיפת אגרגטים של תורם.** `count` כבר נגזר (`donations.length`); `ils/usd`
  מצטברים (בסיס-מיובא + תרומות). ההיסטוריה הלגאסית ב-`hist[]` נפרד. **השאלה:** האם
  הכותרת (count/ils/usd, סף-סיכון, קבלות) צריכה לכלול את `hist[]` הלגאסי, או רק את
  `donations[]` המקובלות? אכיפה עיוורת = איפוס בסיס-מיובא של תורם ותיק.
- **5.5d — פורמט קבלת §46 דיגיטלית.** הקבלה כבר נושאת שם-ארגון, מס׳-עמותה, מס׳-קבלה,
  תאריך עברי+לועזי, סכום+מילים, ת"ז-תורם, נוסח §46, קו-חתימה. **השאלה לרו"ח:** האם נדרש
  סימון "מקור/העתק", פורמט-מספור מסוים, או האם .txt כשר (מול PDF).

## אבטחה — מפה למשתמש (נכתב לבקשת הבעלים)

1. **הצפנה-במנוחה (AES-GCM, envelope, DEK עטוף-כפול)** — קיימת ודורמנטית, **כבויה בכוונה**
   (הכרעת-בעלים: מפתח-ההצפנה שייך לבעל-הנתונים=הלקוח, לא לפלטפורמה). הפעלה = חלון-בעלים.
2. **PBKDF2 600K** (עלה מ-210K, מעל המלצת OWASP) + **הגבלת-קצב** על מסך-PIN (3→5ש׳/4→15/5+→30).
3. **בידוד רב-ארגוני** — כל מפתחות ה-localStorage ממורחבי-שם פר-ארגון (`nsLsKey`), כולל נעילת-
   PIN (5.3). נשארים גלובליים במכוון: a11y (פר-אדם).
4. **Firestore Rules v2** בריפו (allowlist מיילים) — ממתין לפרסום-בעלים.
5. **מזהים חסיני-התנגשות** (#5) — מונע דריסת-רשומות בסנכרון בין-מכשירי.

מקורות: `ANALYSIS-2026-07-29.md` §5 · `RUNBOOK-CLOUD-ENCRYPTION-2026-08-02.md`.
