# 🔍 ביקורת ארכיטקט — SIGNUP (מסך ההרשמה של אורביט)

**מאת:** הארכיטקט · 31.7.2026 · **נבדק:** `מיתוג 1·`–`מיתוג 4·` מול BUILD-ORDER-SIGNUP.

## וורדיקט: ✅ מאושר בתנאי — תיקון ביצועים אחד לפני פריסה

### מה שנכון (אומת עצמאית)
- **אפס CDN בבנדל** — grep על `dist/` נקי (unpkg/cdnjs/googleapis); `three` נארז מקומית; העיתון self-contained.
- **אפס הבטחת-אימות שאין** — Google/Apple/Passkey/WebAuthn/SOC2 הוסרו מהמקור (grep נקי); אימייל+סיסמה בלבד, מחובר ל-cloudSignUp הקיים.
- **בלוק Rules ל-leads בנוי נכון** — create ציבורי מוגבל ל-5 שדות (טלפון+שם חובה-מחרוזת); read/delete למיילי-על בלבד ⇒ לקוח לא קורא לידים.
- **שער-הענן הקיים לא נשבר**; סריקה פורנזית נקייה.
- **שערים:** verify — **856 בדיקות** ✓ · toggle-matrix ✓ · launch 13/13 ✓ · demo 100/100 ✓ · signup.mjs (הכדור עולה, אפס שגיאות JS) ✓.

### 🟠 ממצא ביצועים אמיתי (הבדיקות לא תופסות אותו)
**‏three.js בבנדל הראשי** (‏WebGLRenderer ×5 ב-`index-*.js`; אין `import()` דינמי). הבנדל הראשי קפץ ‎~845K → **~1.5MB**. המשמעות: **כל טעינת אפליקציה** — כולל הלקוח הקיים (מאור החסד) שנכנס ישר ולא רואה את ה-Hero — מורידה ~650K three שלעולם לא בשימוש. נסיגה מול ה-local-first.

**התיקון (קטן, אשכול `מיתוג 5 ·`):** lazy-load — `three-scene`/`SignupHero` דרך `import()` דינמי (או `React.lazy`), כך ש-Vite מפצל את three ל-chunk נפרד שנטען **רק** כשה-Hero מתרנדר. הבנדל הראשי חוזר ל~845K; מסך ההרשמה טוען את ה-3D on-demand (חד-פעמי בלבד — מקובל). ratchet: בדיקת-מקור ש-three מיובא דינמית בלבד, לא סטטית ב-LoginScreen.

### שאלות פתוחות (הבנאי — נכון שרשם)
1. **פרסום Rules מעודכן** — בלוק platformLeads נוסף; הבעלים מדביק את הגרסה מהריפו.
2. **"צפו בסרטון"** — טוסט "בקרוב"; יש סרטוני סיור מוכנים — לחבר?
3. **אשף 5-השלבים** — לא הוטמע; לזריעת platformRequests בהמשך.
