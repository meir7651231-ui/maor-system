# ✅ מה נסגר — חבילת P0 במלואה (דוח מסירה לארכיטקט)

**מאת:** סוכן הבנייה · 29.7.2026 · **ענף:** `claude/what-do-you-see-bcxttj`
**בסיס פקודה:** `knowledge/BUILD-ORDER-P0-2026-07-29.md` · בוצעה בסדר המחייב P0.2 → P0.1 → P0.3 → P0.4 → P0.5, commit נפרד + push לכל פריט.

## שורת הסיכום

חמשת פריטי P0 ממומשים ודחופים. **השער המלא ירוק:** typecheck נקי · lint ‏0 שגיאות ·
**522/522 בדיקות** (52 קבצים; ‏+43 בדיקות חדשות מעל בסיס ה-479) · build תקין ·
**שלוש סוויטות ה-e2e ירוקות:** toggle-matrix ‏5/5 פרופילים · launch-readiness ‏**13/13**
(כולל שני מסעות חדשים) · demo-walkthrough ‏**18/18** (כולל שלוש בדיקות חדשות), אפס שגיאות JS.

| פריט | commit | דגל |
|---|---|---|
| P0.2 סף סיכון 500 | `פער 4` (6a24ca4) | — (יישור התנהגות + ratchet) |
| P0.1 מסלול מעבר נתונים | `פער 5` (490238f) | `supporters.hist` |
| P0.3 כרטיס משפחה תפעולי | `פער 6` (66f6cec) | `families.cardops` |
| P0.4 גיליון העיניים | `פער 7` (b81c5e2) | `supporters.ayin.sheet` |
| P0.5 ייבוא משפחות 13 עמודות | `פער 8` (0ff9231) | `settings.import.families13` |

---

## P0.2 · סף "משפחה בסיכון" 300→500

**מה נבנה:**
- `src/components/families/lib.ts` — קבוע מיוצא `CRED_RED_THRESHOLD = 500`; `tierOf` משתמש בו.
- `src/components/home/homeData.ts:393` (redCred) ו-`src/store/useApp.ts:652` (טוסט הירידה) — שני ה-300 הוחלפו בקבוע.

**בדיקות שנוספו:** `src/components/families/__tests__/cred-threshold.test.ts` — ‏4 בדיקות:
הקבוע=500; ‏450=red/550=pale כולל הגבול המדויק; ‏450 נספר ב-redCred של attentionItems ו-550 לא; אין פריט כשהכול מעל הסף.

**קבלה:** `grep -rn "300" src/` — אפס שימושי סף אמינות שנותרו.

## P0.1 · מסלול מעבר נתונים מהקובץ החי

**מה נבנה:**
- `src/types/domain.ts` — ‏`Supporter.hist?: {d,a,c}[]` (בלי העלאת DB_VERSION).
- `src/store/persist.ts` (`migrate()`) — נרמול hist (לא-מערך→undefined, איברים בלי d/a מספרי נזרקים); הקשחת orgName/orgSite/orgDonate; **זריעת seq מעל הסיומת הספרתית הגבוהה בכל המזהים** (families+members+enrollments+courses+events+rooms+teachers+supporters — תופס גם `spi<N>`), כדי ש-nextId לא ינפיק id תפוס. ids עם קידומות fr/wd/fb נבלעים ללא שינוי.
- `src/components/supporters/lib.ts` — ‏`supDonEvents` טהור (verbatim לגאסי 1486-1495) + תצוגה ב-`SupporterDetail` מאחורי `supporters.hist` (כבוי = קבלות בלבד, כמו קודם).

**בדיקות שנוספו:** `src/store/__tests__/migrate-legacy.test.ts` (6) — fixture גיבוי v:1 מלא בצורת legacy:152: ספירות זהות, אפס שינוי ids, hist מנורמל, seq≥106, org/ui/teachers/ayin עוברים, מוני קבלות נזרעים. `src/components/supporters/__tests__/supdon-events.test.ts` (4) — המיזוג, first/last, דה-דופ תאריכים, מיון.

**e2e:** ‏`e2e/fixtures/legacy-backup.json` + מסע "ייבוא גיבוי לגאסי" ב-launch-readiness — שחזור מהקובץ → ספירות זהות ב-localStorage וגם על המסך → כרטיס תומכת מציג "מהקובץ ההיסטורי" ו"קבלה D-3".

## P0.3 · כרטיס המשפחה חוזר להיות תפעולי

**מה נבנה (חיווט בלבד — אפס שכפול לוגיקה):**
- `src/components/families/FamilyPanels.tsx` (EnrollPanel) — עמודת פעולות פר-שיבוץ: **ניקוב** (רק plan='punch'; אותן חסימות מוקפא/הסתיים/אין-יתרה→ניהול; אותו `store.punch` + ‏+5 אמינות), **🤒 חיסור** (AbsenceModal המיובא מ-courses), **⚙ ניהול** (ManageModal), **🗑 הסרה** (confirm + `deleteEnrollment`; ה-store מוחק גם את תזכורת התשלום).
- EventsPanel — כפתור **"➕ אירוע"** הפותח EventModal עם famId מאוכלס (prefill הורחב ב-famId).
- `FamilyDetail.tsx` — כרטיסי **"אב/אם (להשלמה)"** באותו תנאי כמו `__pf`/`__pm` של JoinModal; לחיצה פותחת MemberForm מאוכלס מראש (`prefill` חדש: first/gender/isParent).
- דגל אחד לכל הסעיף: `families.cardops`.

**בדיקות שנוספו:** `src/components/families/__tests__/cardops.test.ts` (7) — ratchet ש"ניקוב מהכרטיס עובר דרך אותו store.punch" (used לפני/אחרי, תקרת purchased, התעלמות ממנוי) + הגנות-מקור (?raw): המודאלים מיובאים מ-courses, כל הפעולות מגודרות בדגל, ההסרה דרך deleteEnrollment עם confirm. (אין סביבת DOM בבדיקות היחידה בריפו — "הפעולות מופיעות רק כשהדגל דלוק" נאכף בהגנת-מקור + e2e.)

**e2e:** demo-walkthrough הורחב — יצירת חוג כרטיסייה, שיבוץ מכרטיס המשפחה, **ניקוב מהכרטיס** (10→9 מתוך 10), נוכחות ⚙/➕ אירוע. ‏18/18.

## P0.4 · גיליון העיניים round-trip + paid

**מה נבנה:**
- `src/types/domain.ts` — ‏`AyinCase.paid?: boolean` — **ברמת התיק**, כהכרעת הארכיטקט (legacy:990 ייבוא, legacy:197 ייצוא).
- `src/lib/ayin.ts` (טהור) — ‏`AYIN_SHEET_HEADER` + ‏`ayinSheetRows` (ייצוא verbatim legacy:196-198, קובץ `maor-ayin-eyes.csv`, פסיקים→רווח בתשובה) · `parseAyinSheet` (זיהוי עמודות בהכלה, normName, ‏`yes()=/כן|yes|✓|v|שולם/i`, שורה ריקה מדולגת, miss, שגיאה רק על שם/עיניים — legacy:852-869) · `applyAyinSheet` (log על שינוי eyes, done, paid פר-תיק, answers בדה-דופ+answeredNote, lead→stage='eyes' רק כשלא התקדם, lastTouch תמיד — legacy:983-993; אימוטבילי).
- `src/store/useApp.ts` — פעולת `ayinApplySheet(upds)` דרך setDb (סנכרון ענן חינם) + טוסט הלגאסי.
- UI: `AyinSheetImport` ב-ImportSection — ⬇ הורדה + ⬆ ייבוא עם סיכום דו-שלבי ("זוהו N · X עיניים · Y שולם · Z תשובות · W עופרת" + שורות לא-מותאמות). דגל `supporters.ayin.sheet` (נבדק יחד עם `supporters.ayin`).

**בדיקות שנוספו:** `src/lib/__tests__/ayin-sheet.test.ts` (9) — round-trip מלא ייצוא→שינוי→ייבוא→החלה; eyes=0 תקין; miss; עמודות חסרות; דה-דופ תשובות; טבלת yes(); שולם פר-תיק (ולא על השם); אימוטביליות.

## P0.5 · ייבוא משפחות 13 עמודות

**מה נבנה:**
- `src/lib/familiesImport.ts` (טהור) — ‏`parseFamiliesCsv`: מיפוי 13 עמודות קבוע + **כל הניקויים** verbatim מ-legacy:944-960 (שורת "שם פרטי שם משפחה"; יריד חנוכה→notes; ‏#NAME?; עיר רגיל→''/ביתר→ביתר עילית; `סטטוס:` מהערות→inactive; אלמן/גרוש/נשואים; '-'→'' בטלפונים; קהילה→'חסידי'; כתובת 7+8) + התאמה לקיימות (legacy:960) · `mergeFamilyImport` (רק שדות לא-ריקים, members/docs לא נגעים — legacy:1005).
- `ImportSection.tsx` — ‏`Families13Import`: קובץ/הדבקה → **preview דו-שלבי** ("זוהו N משפחות · +X חדשות · Y עדכונים") → החלה אטומית. דגל `settings.import.families13`; כבוי = מסלול 5 העמודות הישן ללא שינוי.
- `src/lib/csvx.ts` — ‏`readCsvFileText`: ‏helper משותף ל-fallback ‏windows-1255, חובר לכל מסלולי הייבוא (ילדים · תומכות · גיליון מעקב · משפחות 13).

**בדיקות שנוספו:** `src/lib/__tests__/families-import.test.ts` (12) — בדיקה נפרדת לכל ניקוי עם הפניית שורת מקור + התאמה לקיימות (טלפון זהה/שונה/ריק) + מיזוג שדות לא-ריקים.

---

## סטיות מהמפרט (מלאות, עם נימוק)

1. **P0.3 — "בדיקת רנדר שהפעולות מופיעות רק כשהדגל דלוק":** בריפו אין סביבת DOM לבדיקות יחידה (אין jsdom/testing-library; הדפוס הקיים הוא הגנות-מקור `?raw`). הגידור בדגל נאכף בהגנת-מקור על ה-JSX + אימות חי ב-demo-walkthrough. לא הוספתי תלות חדשה בלי אישור.
2. **P0.4 — בדיקת ה-round-trip חשפה התנהגות לגאסי סדר-תלוית:** 'שולם' פר-תיק מיושם שורה-אחר-שורה, כך ששורה מאוחרת של אותו תיק עם 'לא' דורסת 'כן' משורה קודמת. שוחזר נאמנה (זו התנהגות הלגאסי) ותועד בבדיקה.
3. **P0.1 — זריעת seq הורחבה לכל הישויות** ולא רק ל-supporters: הלגאסי מנפיק prefix+seq לכל ישות ('e'/'ev'/'f'/'fi'/'m'/'mi'/'mp'/'r'/'t'/'spi'), ולכן זריעה חלקית הייתה משאירה סכנת התנגשות id. ברוח המפרט ("להרחיב את סריקת המזהים").
4. **commit נוסף `תיקון ·`** (a0540fb): הסרת צילום e2e שנכנס בטעות לשורש הריפו מסקריפט דיבאג. אפס השפעה על קוד.
5. **אזהרות lint על `knowledge/legacy/*.js`** קיימות בבסיס (oxlint סורק גם את קובצי הלגאסי; exit 0, אפס שגיאות): קדם-קיים, הקבצים immutable — לא טופל.

## עדכוני ידע

- `knowledge/legacy/gaps.json` — נוסף `closed: "2026-07-29"` + ‏`closed_by` לארבעה פריטים שנסגרו במלואם (שורת שיבוץ בכרטיס המשפחה · גיליון עיניים · מודל תומכות hist/ayin.paid · היסטוריית תרומות מאוחדת); שני פריטים חלקיים קיבלו הערת עדכון בתוך `detail` (ייבוא רב-סוגי — נותר סיכום-לפני-החלה לתומכות; מנוע האמינות — דלתא הסף נסגרה, השאר הוכרעו כשדרוגי React ל-P1).
- `knowledge/AGENT_CONTEXT.md` — סעיף "על מה אני עובד עכשיו" עודכן: P0 נמסר.

**מוכן לפקודת בנייה P1.**
