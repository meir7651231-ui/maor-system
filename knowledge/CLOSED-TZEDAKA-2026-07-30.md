# CLOSED-TZEDAKA — דוח מסירה · עמודת "קופות צדקה"

**תאריך:** 2026-07-30 · **ענף:** `claude/what-do-you-see-bcxttj` · **מנדט:** `knowledge/BUILD-ORDER-TZEDAKA-2026-07-30.md`

## שורת סיכום
כל 8 האשכולות בוצעו לפי הסדר (קומיטים `קופות 1·`–`קופות 8·`). בדיקות 672 → **695** (0 אדומות) · 106 דגלים (נוספו 5) · 37 מונחים (נוספו 4) · 11 מערכי ישויות (נוספו 4) · שלוש סוויטות הדפדפן ירוקות כולל הזרימה החדשה (רכז → קופה → ריקון 100₪).

## טבלת פריט-מול-סטטוס

| # | פריט הפקודה | סטטוס |
|---|---|---|
| 1-7 | שכבת נתונים: 4 ישויות, מיגרציה אדיטיבית (DB_VERSION=5), ריפוי, cloud-diff/merge + 5 בדיקות | ✅ `קופות 1` |
| 8-16 | שלד: ModuleKey/View/NAV/VIEWS/builder-section, 5 דגלים, 4 מונחים, TzedakaView 4 טאבים + ratchet שרשור | ✅ `קופות 2` |
| 17-19 | מנוע טהור (ניקוד/סכומים/staleBoxes/needsCare/leaderboard/campaignProgress/buildTzGrid) + 10 פעולות store + ratchet-בידוד | ✅ `קופות 3` |
| 20-25 | CoordinatorsTab/CoordinatorCard (קופות מקוננות + פעולות) /CoordinatorForm/BoxForm/CollectModal + inline-create + ratchet | ✅ `קופות 4` |
| 26-29 | HomeTab: מדדים, דורש-טיפול עם פעולות, ניהול מבצעים + פס התקדמות, קיצורים | ✅ `קופות 5` |
| 30-32 | CalendarTab (עברי כברירת מחדל, צבעי calLib) + TzEventModal + הגנת-מקור בידוד | ✅ `קופות 6` |
| 33-34 | ShowcaseTab: טבעת RING, fmtIls מיובא, מובילים 🥇🥈🥉, מסך-מלא dark-luxe בלי hash | ✅ `קופות 7` |
| 35-37 | e2e (tzedaka:false בשני המערכים + קישור קיים/נעדר + זרימה מלאה), דוח, CLAUDE.md | ✅ `קופות 8` |

## הכרעות הבעלים — מימוש
1. **אין גישת רכזים** — אפס עבודת roles; הכול משרדי. ✅
2. **כרטיסי רכזים בדפוס מורים + קופות מקוננות בדפוס חוגים.** ✅
3. **בית/לוח/ראווה בתוך העמודה** — 4 טאבים, אין משטח חדש מחוץ לה. ✅
4. **בידוד מלא** — נאכף גם ב-ratchets: `addTzCollection` לא נוגע ב-donationSeq/receiptSeq/supporters/enrollments/events; `dayItems()` של הלוח הראשי לא רואה tzEvents; CollectModal בלי שום ייבוא מ-supporters/receipt; CalendarTab בלי upsertEvent; הראווה בלי hash. דלת עתידית: הנתונים ב-Db ובענן — חיבור עתידי = קריאה מהמערכים הקיימים. ✅
5. **ניקוד גיימיפיקציה** — TZ_SCORE_RULES (‏+10 לריקון, ‏+1/50₪, רצף 60 יום ‎+5; קבוע אחד לכוונון הבעלים) + לוח מובילים + כוונון ידני. ✅
6. **הוספת לא-רשומים** — משפחה חדשה (עם סיווג תורמת/נתמכת) וילד/הורה חדש דרך `upsertFamily`/`upsertMember` הקיימים, מאחורי `tzedaka.inlinecreate`. ✅

## סטיות ופרשנויות קטנות (לא הכרעות מוצר)
1. **סיווג משפחה חדשה:** למודל Family אין שדה סיווג — הסיווג נשמר כ-holderKind על הקופה, וביצירת משפחה חדשה נרשם גם כטקסט בהערות הכרטיס ('סווגה תורמת/נתמכת (קופות צדקה)'). אם רוצים שדה CRM ייעודי — שאלת מוצר לבעלים.
2. **"➕ קופה" מקיצורי הבית** נפתח על הרכז הראשון ברשימה (הטופס עצמו אינו כולל בחירת רכז — הקופות נולדות בכרטיס הרכז בדפוס המקונן). ההוספה המלאה — מכרטיס הרכז.
3. **צבעי kind בלוח הייעודי:** round לפי SESSION_META (כמו בפקודה); campaign/reminder/custom ממופים ל-PRIORITY_COLOR red/orange + כחול הקבוע — הפקודה לא פירטה מיפוי מדויק פר-kind.

## שאלות מוצר שנרשמו (לא הוכרעו)
- האם לחשוף בעתיד את סכומי הקופות בדוחות/קבלות — "אחר כך נעדכן" (הכרעה 4); הארכיטקטורה מוכנה (הנתונים ב-Db + ענן).
- שדה סיווג תורמת/נתמכת קבוע על Family (ראו סטייה 1).

## שערים — מצב סופי
`verify:fast` ✓ ‏(695 בדיקות, ‏+23) · build ✓ · toggle-matrix ✓ (כולל: קישור קיים בברירת מחדל, נעדר בכבוי, רכז+קופה+ריקון 100₪ והסכום מופיע, אפס דליפות בקיצון) · launch-readiness 13/13 ✓ · demo-walkthrough 100/100 ✓ — **אפס נגיעה בהתנהגות מסכים קיימים, כבוי ודלוק.**
