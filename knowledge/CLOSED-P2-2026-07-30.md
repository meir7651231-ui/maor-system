# CLOSED-P2 — דוח מסירה · חבילת P2 (פערים 19–33) + מעבר 199/199

**תאריך:** 2026-07-30 · **ענף:** `claude/what-do-you-see-bcxttj` · **בנאי:** סוכן הבנייה (סשן ב׳)
**מנדט:** `knowledge/BUILD-ORDER-P2-2026-07-30.md` + `BUILD-ORDER-P2-MICRO.md` (המיקרו-ספק גובר לפריטים שנותרו)

## שורת סיכום
כל 15 הפערים (19–33) סגורים · בדיקות 588 → **647** (0 אדומות) · שלוש סוויטות הדפדפן ירוקות בסוף כל אשכול ובסוף החבילה · **מעבר 199/199 בוצע: 53 present + 53 closed + 23 מדווחים לארכיטקט** (אפס השמטות שקטות) · 98 דגלי פיצ׳ר (היו 73).

## ⚠️ שני בנאים במקביל — שקיפות
בסשן פעל בנאי מקביל על אותו ענף. פרוטוקול העבודה: fetch לפני כל push, אימוץ העבודה המרוחקת כבסיס, והשלמת פערים בקומיטים משלימים. חלוקה בפועל:
- **בנאי מקביל:** פער 19-20 (בסיס, `8f414a4`) · פער 21 (`fa667e5`) · מסמכי ידע (P2-MICRO, ARCHITECT-LESSONS).
- **סשן זה:** השלמות פער 20 (אריחים לחיצים, ספרייה, דמו) · פערים 22–33 · יישור ל-P2-MICRO · מעבר 199/199 · דוח זה.

## מה נסגר — פר-אשכול

### א׳ · מדדי הבית (פער 19-20)
- `home.coursemetrics` — תפוסה + הכנסה משוקללת (punch=0) + Top-3; `home.credmetrics` — מד מחוג, היסטוגרמה, דורשות חיזוק.
- השלמות סשן זה: אריחי דרגות לחיצים (titan/lion/pale/red → מסך המשפחות מסונן, famTierReq ב-store), מונה אלמנות, רישום ב-WIDGET_LIBRARY (תיקן שבירת homeData.test), פריסות ברירת מחדל, צעדי דמו.

### ב׳ · כפילויות וביקורת (פער 21-22)
- פער 21 (בנאי מקביל): מיזוג שדה-שדה 18 שדות + dupDrop, דגל `settings.dedup.fields`.
- פער 22: `runAudit(db, todayIso, extra)` — יעד-קשר שעבר (קשר) + תרומה ≤0 (לוגיקה), טהור עם todayIso מוזרק; `auditToAttention` (📌 → תזכורת אדומה להיום, כמו sendAuditToCare); 🔀 מכפילות ל-#dedup. דגל `settings.audit.extra`.

### ג׳ · דוחות וייצוא (פער 23-24)
- פער 23: `expFieldDefs` מלא — קורסים 14 · תומכות 17 (verbatim legacy:1699-1706); studentsFull (שם+טלפון+יתרה), revenue, donsAll, tier, eyesTotal, paid; טווח עברי חי; תצוגה מקדימה עם עריכת הערות (`overrideColumn` — לייצוא בלבד); בחר-הכל/נקה. דגל `reports.custom.full`. domain: ‏`Supporter.city?`‏, `Course.gradeMin/Max?/img?`.
- פער 24: `eventsCsvRows` (9 עמודות verbatim כולל עברי+עדיפות); כפתור "⬇ אירועים"; השלמת כל 16 עמודות הלגאסי בייצוא החוגים; פעולת "⬇ ייצוא CSV" בפלטה (dlCSV — חוב P1). דגל `reports.export.full`.

### ד׳ · לוח שנה (פער 25-26)
- פער 25 (הכרעה 6): `upcomingItems` — 30 יום, שכבות עבריות דרך dayItems (בלי מפגשי חוגים), מכבד את פילטרי הלוח; צ׳יפ משפחה לחיץ לפי famId עם fallback לצבע-סוג; ✎/✓ בוצע נשמרים. דגל `calendar.upcoming` (כבוי = רשימת 14 הימים הישנה).
- פער 26: `quickDate` + צ׳יפים היום/מחר/בעוד שבוע/בעוד חודש בטופס האירוע (בלי דגל, עם ratchet).

### ה׳ · חוגים (פער 27-28)
- פער 27: `roomsNow` (now מוזרק, slot ברירת מחדל 60ד׳) + רצועת 🟢/🔴 בראש מסך החוגים, רענון דקתי. דגל `courses.roomslive`.
- פער 28: GRADE_ORDER גן–י"ב, `gradeIndex`/`gradeFits` (רך — מידע חסר לא מסתיר), משתלב ב-P1.7 בשני מסלולי השיבוץ; טופס: בוררי כיתות + העלאת תמונה dataURL; כרטיס: תמונה + שורת כיתות. דגל `courses.gradeimg`.

### ו׳ · מעטפת (פער 29-31)
- פער 29: `lib/guide.ts` טהור — התוכן מילה-במילה (markup:2891-2913); GuideModal נגיש; כפתור 📖 בכותרת + פלטה + `#guide`. דגל `shell.guide`.
- פער 30 (הכרעה 4): `lib/tour.ts` — 12 צעדים לפי runDemo עם כיתובים מילה-במילה, `tourAdvance`/`spotlightBox` טהורים; TourOverlay עם spotlight אמיתי, הבא/הקודם/חיצים, "■ עצירת הדמיה (Esc)"; **לא** פורט האוטו-קליקר. כפתור ▶ בכותרת + פלטה + `#tour`. דגל `shell.demo`.
- פער 31: `lib/a11y.ts` טהור (0.8–1.6 בצעדי 0.1 עם עיגול הלגאסי — התקרה הועלתה מ-1.5); `a11yApply.ts` משותף; FAB ♿ עם 4 המתגים בנוסח הקבוע, איפוס, "הבחירות נשמרות אישית בדפדפן זה". דגל `shell.a11yfab`.

### ז׳ · חובות (פער 32-33)
- פער 32 (הכרעה 1): `families.cred.trendCreditsOnly` — חסר/true = React (מקדם רק על זיכויים); false = לגאסי (גם עונשים, legacy:387). ratchet לשני המצבים.
- פער 33: SupporterImport דו-שלבי (analyze→apply, setDb רק ב-apply); דגל `supporters.import.preview` (false = החלה מיידית).

### ח׳ · סגירות-במקום מהמעבר
- כל 16 עמודות ייצוא החוגים (טלפון מורה, קבוצות, הנחות, גיל).
- `nextOccurIso` (לגאסי nextOccurLabel) — "המופע הבא" בטופס אירוע חוזר.
- קישור "לכרטיס החוג ←" ב-ManageModal.

## מעבר 199/199 — מצב הצילום המלא
`gaps.json` = 129 פריטי-פער ממופים (המספר 199 בהנחיה מקורו בספירה המשולבת עם inventory.json — ל-138 ההתנהגויות ב-inventory אין שדה סטטוס פר-פריט; הכיסוי שלהן נבדק דרך המיפוי ל-gaps.json):

| מצב | כמות |
|---|---|
| present (פורט נאמן מהיום הראשון) | 53 |
| closed (P0+P1+P2, עם `closed_by` מנומק) | 53 |
| **נותרו פתוחים — מדווחים כאן, לא הוכרעו לבד** | **23** |

### 23 הפריטים שנותרו — דיווח לארכיטקט (לפי ההנחיה: לא מכריעים לבד על השמטה)
רובם "שאריות UI" קטנות שהעדות שלהן מראה כיסוי כמעט מלא. מקובצים לפי המלצה:

**קבוצה 1 — מועמדים ל-P3 קצר (שאריות קטנות ומוגדרות):**
1. courses/ריבוי מפגשים — הגזירה האוטומטית מטקסט audience (regex 'קבוצות|פעמים') לא קיימת; קבוצות רק ידנית. שקול תפעולית.
2. courses/טבלת קורסים — בפילטר המסלול חסרות אפשרויות חצי-שנתי/שנתי.
3. courses/כרטיס קורס — חסר "🔔 תזכורת ללוח" ליצירת אירוע לחוג.
4. data/ייצוא בפורמט הייבוא — תבניות round-trip מלאות למשפחות/תומכות (לעיניים קיים מ-P0.4).
5. events/שמירת אירוע — האזהרה הרכה ' · ⚠ התנגשות עם "X"' בטוסט (הקשיחות קיימות).
6. events/תצוגת יום + תצוגה יומית (2 פריטים) — שורת המשנה (מורה · חדר · N רשומים / notes) בשורות הציר.
7. events/מרכז הטיפול — איפוס גורף של סימוני "טופל" (אריחי הדרגות כבר נוספו בפער 20).
8. families/פאנל אמינות בכרטיס — כפתור "איך משפרים?" (שורת כללי הניקוד).
9. families/ציר זמן — שזירת אירועי הלוח (כולל ✓) בהיסטוריה.
10. shell/ניהול מורות — רשימת חוגי המורה + "📞 תזכורת קשר" בכרטיס.
11. supporters/כרטיס תומכת + supModal (2 פריטים) — לחיצה על תרומה מסמנת את היום בלוח האישי; קיטום היסטוריה ל-60. (🧾 פר-תרומה ויעד-בלי-confirm כבר נסגרו ב-P1.)
12. supporters/פאנל דרגות + מסך 💛 (2 פריטים) — היסטוגרמת ציון, ממוצע-לתרומה (×3.7), מונה 12 חודשים, 📅 לוח כלל-ארגוני מהמסך (קיים ב-P1.4 מהכרטיס), 📞 פר-שורה.
13. supporters/טבלת תומכות — פילטרים פר-עמודה בתחביר numMatch.
14. supporters/ayin בטבלה — סינון eyes/noeyes/'עודכן היום' + סימון "שולם" בטבלה (paid קיים במודל מ-P0.4).

**קבוצה 2 — דורש הכרעת ארכיטקט (עיצוב/ארכיטקטורה):**
15. shell/role מנהל/מורה — מודל ההרשאות הוחלף (PIN דו-שכבתי + adminEmails). אין תפקיד "מורה" שמסנן קורסים. **אם יש לקוח שמפעיל role='מורה' — פער אמיתי**; דורש החלטה אם לבנות תפקידים או להסתפק בנעילות.
16. shell/מרכז הטיפול (view care) — אין מסך ייעודי; הליבה קיימת כווידג'ט משופר בבית + 📌 מהביקורת (פער 22). הכרעה: האם ווידג'ט = היכולת, או שנדרש מסך.
17. families/כרטיס משפחה מלא — פעולות השיבוץ בכרטיס קיימות מ-P0.3 מאחורי `families.cardops`; הפריט מתעדכן ברובו — נותר אימות סופי מול רשימת ה-detail הישנה.
18. families/רשימת משפחות — נקודות הכניסה "⬆ ייבוא"/"✓ בדיקת נתונים" הועברו להגדרות (בכוונה?); "✦ מצא חוג" קיים כגלגל. הכרעה: להחזיר קיצורים למסך או לא.
19. families/armDel — window.confirm במקום שני-קליקים-עם-פקיעה. הבטיחות קיימת; UX שונה. הכרעה סגנונית.
20. shell/פלטה ליבה — קיבוץ-לפי-סוג קיים (P1.6) אך כלל ציון-130 והצעות-6 שונים. שקילות כמעט מלאה; נותר אימות.
21. events/מודל אירוע markup (פריט "תצוגה יומית — מבנה") — חופף לפריט 6 לעיל.

**המלצה:** קבוצה 1 = חבילת P3 קצרה (~10 פריטים קטנים, יום עבודה); קבוצה 2 = 5 הכרעות ארכיטקט.

## סטיות מהספק — מתועדות, לא הוכרעו בשקט
1. **פער 26:** סט הצ׳יפים לפי המיקרו-ספק (היום/מחר/שבוע/חודש) — הלגאסי המקורי כלל היום/שבוע/חודש/חודשיים/3 חודשים. המיקרו-ספק גובר; הרחבה טריוויאלית אם יוחלט.
2. **פער 31:** הזום מוחל ב-body zoom (כל הממשק) ולא "טקסט בלבד" כמו monkey-patch הלגאסי — טכניקת הלגאסי לא ישימה ב-React עם מידות px; לכן גם התווית קוצרה ל"גודל אותיות". ה-FAB מוקם ב-inline-end (הפינה הנגדית לטוסטים — כמו הפרדת הפינות בלגאסי).
3. **פער 23:** `Supporter.city` נוסף למודל ולייצוא, אך אין עדיין שדה עיר בטופס התומכת — העמודה תתמלא מייבוא/עריכה עתידית. מומלץ להוסיף input בטופס ב-P3.
4. **פער 30:** לפי הכרעה 4 — סיור מודרך ולא אוטו-קליקר; העוגנים נמצאים לפי טקסט וכשלא נמצאו הכיתוב מוצג לבדו (עיצוב עמיד).

## שערים — מצב סופי
- `verify:fast`: typecheck ✓ · lint 0 warnings ✓ · **647 בדיקות** ✓ (base 479 → P0 522 → P1 588 → P2 647).
- build מלא בכל push (pre-push hook) ✓.
- toggle-matrix 5/5 ✓ · launch-readiness 13/13 ✓ · demo-walkthrough 100/100 ✓ (כולל הצעדים החדשים: מדדים, מדריך, פאנל קרובים) — בסוף כל אשכול ובסוף החבילה.

## עדכוני CLAUDE.md בקומיט זה
- מונה הדגלים: 73 → **98** (33 מונחים ללא שינוי).
- הפניה לדוח זה.
