# 🔍 ביקורת ארכיטקט — מסירת עמודת "חנות" (מודול shop)

**מאת:** הארכיטקט · 30.7.2026 · **נבדק:** קומיטים `חנות 1·`–`חנות 8·` (‏72b77c8…ffa7a90) מול `BUILD-ORDER-SHOP-2026-07-30.md`.

## וורדיקט: ✅ המסירה מאושרת

אימות עצמאי (לא הסתמכות על דוח הבנאי):
- **קוד מול פקודה:** כל 35 הסעיפים ממומשים — 5 ישויות מילה-במילה, מיגרציה אדיטיבית עם ריפוי (discountPct קטום 0-100), ענן 16 אוספים + מערכים מקוננים, שלד מודול שמיני, מנוע טהור, 11 פעולות store, 5 טאבים, inline-create בדפוס הקופות.
- **רפקטור הגריד נקי:** `buildTzGrid` הפך ל-wrapper דק על `lib/monthGrid.ts` הגנרי; **קובצי הבדיקה של הקופות לא שונו כלל** (אומת ב-git diff) וכולם ירוקים — בדיוק תנאי סעיף 14.
- **הבידוד (הכרעה 6) נאכף ב-ratchets:** addShopRedemption מוכח לא-נוגע ב-donationSeq/receiptSeq/supporters/enrollments/events; ‏dayItems() עיוור ל-shopEvents; הגנות-מקור על CalendarTab/RedeemModal/ShowcaseTab.
- **מנוע המחיר מדויק לפקודה:** ‏25%/60% ⇒ 40 (הגבוהה, לא מצטבר); עיגול 66.33⇒66; לא-שלילי; מקרי מתנת-חג לפי חג+שנה.
- **סריקה פורנזית:** אפס ‎.skip/.only/.todo/בדיקות ריקות.
- **שערים בהרצה עצמאית שלי:** verify מלא + **730/730 בדיקות** (98 קבצים) ✓ · toggle-matrix כל הפרופילים כולל זרימת מוצר-חתן ✓ · launch-readiness 13/13 ✓ · demo-walkthrough 100/100 ✓.
- ידע עודכן כנדרש: CLAUDE.md (111 דגלים · 42 מונחים · 16 מערכים + שורת מודול), DECISIONS (טבלת הכרעות החנות), ה-placeholder המיושן בקופות נמחק (סגירת הליקוי מ-AUDIT-TZEDAKA).

הערה טכנית: הבנאי דחף גם לענף הסשן שלו `claude/p0-bundle-data-migration-vg7z6b` (אותו קומיט בדיוק) — כפילות לא מזיקה.

## שאלות מוצר פתוחות (הבנאי לא הכריע — נכון; ההכרעה לבעלים)
1. **מלאי/כמויות לרכיבים** — כרגע אין מונה מלאי (כמה סטים של מתנה נשארו). להוסיף?
2. **קבלה על התשלום הסמלי** — כרגע אין קבלה (בידוד); חיבור עתידי לקבלות = הכרעת "אחר כך נעדכן".
3. **תוקף לקופונים** — כרגע קופון בלי תאריך תפוגה. להוסיף?

סטייה קטנה שאושרה בביקורת: צ'יפ "🎁 ממתינות" בתאי הלוח מחושב פר-תא בלי מגבלת 30 הימים — נכון יותר לדפדוף לחודשים רחוקים.
