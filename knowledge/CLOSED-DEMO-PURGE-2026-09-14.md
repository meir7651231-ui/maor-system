# 🧹 הסרת נתוני-הדמו — חקירה + סגירה (14.9.2026, ‏#492)

**בקשת-בעלים:** "איפה אני מאפס את הנתונים דמו שנכנסו".

## 1 · מה נמצא
- "📊 טעינת נתוני דמו" (`DemoDrop`) מוצג כשהמאגר-המקומי ריק (`famCount === 0`). במכשיר-ענן חדש המאגר ריק
  לרגע לפני שהמשיכה מהענן מסתיימת ⇒ הכפתור נראה, לחיצה עשתה `restoreDb(demo)` והסנכרון **מיזג** (איחוד-ids)
  את הדמו לתוך הנתונים האמיתיים בענן.
- הכלי היחיד שהיה: הגדרות ← 🧪 מתקדם ← "🗑 איפוס" — מוחק **הכול**, גם בענן ובכל המכשירים. לא הכלי הנכון לדמו שהתערבב.
- מזהי-הדמו קבועים בקובץ (`f128…`, `sp639…`, `c114…`, `ev614…`, `t100…`) ⇒ הסרה סלקטיבית מדויקת אפשרית.

## 2 · מה נבנה
- `src/lib/demoPurge.ts` (טהור): `demoIdSets` · `planDemoPurge(db, demo)` — מסיר רק רשומות שמזהיהן בקובץ-הדמו,
  בכל אוסף-מערך (משפחות/שיבוצים/חוגים/אירועים/חדרים/מורים/תורמים/קופות/חנות/חלוקה); רשומות אחרות ביט-זהות; המונים לא נגעים.
- סטור: `purgeDemoData` (שער-מנהל בענן · `withRemovalTombstones` ⇒ הענן ומכשיר-אופליין לא מחזירים · לוג · טוסט) · `previewDemoPurge`.
- הגדרות ← 🧪 מתקדם ← איפוס: בלוק **"🧹 הסרת נתוני-הדמו בלבד"** — 🔍 בדיקה ("נמצאו N רשומות-דמו: משפחות 60 · תורמים 15 · …")
  ⇒ הסרה בשתי לחיצות. בלי מנוי-מאגר-מלא (ratchet המנויים הממוקדים — הבדיקה דרך הסטור).
- App: `DemoDrop` מוסתר כשיש `firebase` ו-`slug !== 'demo'` — הטעות לא תחזור; הדמו הציבורי נשאר.
- ratchets: `demo-purge.test.ts` (7) · `e2e/demo-purge-smoke.mjs` (10/10 — נזרע לפני הטעינה הראשונה: IndexedDB גובר על localStorage).

## 3 · הפעלה אצל הבעלים
הגדרות ← 🧪 מתקדם ← איפוס ← "🔍 בדיקה" ← "🧹 הסרת N רשומות-הדמו" (שתי לחיצות). בענן: ההסרה מסתנכרנת לכל המכשירים.

## לקחים
- כפתור-זריעה שמגודר "מאגר ריק" הוא מלכודת בלקוח-ענן (ריק-לרגע). גידור: `!(config.firebase && slug !== 'demo')`.
- ratchet המנויים: גם המילה `db` בהערה נתפסת — בקבצי `useDbWatch` לא כותבים `db` חופשי.
