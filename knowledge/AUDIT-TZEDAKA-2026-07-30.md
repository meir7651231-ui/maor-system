# 🔍 ביקורת ארכיטקט — מסירת עמודת "קופות צדקה"

**מאת:** הארכיטקט · 30.7.2026 · **נבדק:** קומיטים `קופות 1·`–`קופות 8·` (‏4205cca…9e3551c) מול `BUILD-ORDER-TZEDAKA-2026-07-30.md`.

## וורדיקט: ✅ המסירה מאושרת

אימות עצמאי (לא הסתמכות על דוח הבנאי):
- **קוד מול פקודה:** כל 37 סעיפי הפקודה קיימים בקוד — טיפוסים מילה-במילה, מיגרציה אדיטיבית (DB_VERSION נשאר 5) עם ריפוי, cloud-diff (11 אוספים) + cloud-merge (מערכים מקוננים), שלד מודול שביעי עם שרשור-כיבוי, מנוע טהור, 10 פעולות store עם כל השערים, 4 טאבים, inline-create (הכרעה 6).
- **הבידוד (הכרעה 4) נאכף ב-ratchets אמיתיים:** קראתי את הבדיקות עצמן — addTzCollection לא נוגע ב-donationSeq/receiptSeq/supporters/enrollments/events; ‏dayItems() של הלוח הראשי מתעלם מ-tzEvents; הגנות-מקור על CollectModal (אפס import מעולם הקבלות) ו-CalendarTab (אפס upsertEvent/db.events).
- **סריקה פורנזית:** אפס ‎.skip/.only/.todo, אפס בדיקות ריקות בקבצי הבדיקה החדשים.
- **שערים בהרצה עצמאית שלי:** verify מלא — 695/695 + build ✓ · toggle-matrix — כל הפרופילים כולל הזרימה החדשה (רכז→קופה #77→ריקון 100₪) ובדיקת ההיעדר בכבוי ✓ · launch-readiness 13/13 ✓ · demo-walkthrough 100/100 ✓.

## ליקוי קוסמטי (לא חוסם — לתיקון בנגיעה הבאה של הבנאי)
- `src/components/tzedaka/TzedakaView.tsx:48` — הערה מיושנת: "התוכן נבנה באשכולות 4-7 — placeholder לכל טאב עד אז". התוכן בנוי; ההערה למחיקה.

## שלוש שאלות מוצר פתוחות (הבנאי נהג נכון — לא הכריע לבד; ההכרעה לבעלים)
1. **סיווג תורמת/נתמכת** נשמר כ-holderKind על הקופה בלבד — לא שדה קבוע בכרטיס המשפחה (ל-Family אין שדה כזה). רוצים שדה CRM קבוע?
2. **"➕ קופה" מקיצורי מסך הבית** נפתח על הרכז הראשון (ההוספה הטבעית — מכרטיס הרכז).
3. **צבעי הלוח הייעודי** ל-campaign/reminder/custom נגזרו מ-PRIORITY_COLOR הקיים (הפקודה פירטה רק את round).

סטטוס: ממתינות להכרעת בעלים; ברירות-המחדל של הבנאי סבירות ואינן חוסמות שימוש.
