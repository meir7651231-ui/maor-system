# 🏁 פקודת בנייה P3 — סגירת 23 הנותרים → 129/129

**מאת:** הארכיטקט · 30.7.2026 · **ענף:** `claude/what-do-you-see-bcxttj` · כללי P2 בתוקף (שערים מדורגים, דגל+termOf, ratchet, commit `פער N ·` נפרד). מספור הפריטים לפי CLOSED-P2 §"23 הנותרים".
**הכרעות מוטמעות:** role=הכרעה 2 של הבעלים (roles ב-OrgConfig). מרכז-טיפול=הווידג'ט הוא צורת השדרוג (המלצת ארכיטקט — מסך נפרד לא נבנה). armDel=מיושר ללגאסי. קיצורי-ייבוא=חוזרים מאחורי דגל. הבעלים רשאי לדרוס כל אחת — לתעד ב-DECISIONS.

## אשכול א׳ · תפקיד מורה (פריט 15 — מימוש הכרעה 2)
**קבצים:** `types/config.ts` · `lib/config.ts` · `App.tsx` · `CoursesView/CourseDetail` · דגל `shell.roles`.
1. `OrgConfig.roles?: { teachers?: Record<string /*email*/, string /*teacherId*/> }` (adminEmails קיים — לא לגעת).
2. helper טהור `roleOf(config, email): 'admin'|'teacher'|'staff'` ב-lib/config: email ב-adminEmails ⇒ admin; במפת teachers ⇒ teacher; אחרת staff.
3. מורה מחוברת (cloud.user.email): מסך החוגים מסונן לחוגים עם teacherId שלה + מוסתרות פעולות מחיקה/הגדרות/ייבוא (adminOnly). בלי ענן/בלי roles — התנהגות של היום בדיוק.
4. **בדיקות:** roleOf (4 מקרים) + סינון החוגים טהור. e2e לא נדרש (ענן כבוי בסוויטות).

## אשכול ב׳ · שאריות UI (פריטים 1-14, קומיט לכל 2-3 קרובים)
5. **פריט 2** — פילטר מסלול בטבלת קורסים: הוסף אפשרויות `half_year`/`year` (התוויות הקיימות של planWord).
6. **פריט 3** — "🔔 תזכורת ללוח" בכרטיס חוג: יוצר OrgEvent type='reminder' עם שם החוג + ניווט ללוח. reuse של upsertEvent.
7. **פריט 5** — אזהרת התנגשות רכה בטוסט שמירת אירוע: `' · ⚠ התנגשות עם "X"'` כשיש אירוע אחר באותו יום+שעה (לא חוסם — החסימות הקשיחות קיימות).
8. **פריט 6+21** — שורת משנה בציר היום: `מורה · חדר · N רשומים` למפגש, `notes` לאירוע (DayModal + תצוגת יום).
9. **פריט 7** — "איפוס סימוני טופל" בווידג'ט הטיפול: כפתור קטן שמנקה attnDone (עם אישור דו-קליק).
10. **פריט 8** — "איך משפרים?" בפאנל האמינות: מודאל עם שורת כללי הניקוד. **קרא לגאסי:** grep `credHelp` — העתק את הכללים מילה-במילה.
11. **פריט 9** — ציר-זמן משפחה: שזירת אירועי הלוח של המשפחה (famId) בהיסטוריה, כולל ✓בוצע.
12. **פריט 10** — כרטיס מורה: רשימת החוגים שלה (סינון לפי teacherId) + "📞 תזכורת קשר" (OrgEvent call).
13. **פריט 11** — כרטיס תומכת: לחיצה על תרומה מסמנת את יומה בלוח האישי; קיטום תצוגת ההיסטוריה ל-60 (slice בלבד — לא מחיקת נתונים!).
14. **פריט 12** — פאנל דרגות תומכות: היסטוגרמת 10 סלים של 100, ממוצע-לתרומה, מונה 12-חודשים, כפתור 📅 מהמסך (מנוע P1.4 קיים), 📞 פר-שורה. פונקציות טהורות ב-supporters/lib.
15. **פריט 13** — פילטרים פר-עמודה בטבלת תומכות בתחביר numMatch. **קרא לגאסי:** grep `numMatch` — העתק את הפרסר + ratchet עליו.
16. **פריט 14** — סינון ayin בטבלה: eyes/noeyes/עודכן-היום + עמודת "שולם" (paid קיים במודל).
17. **פריט 1** — גזירת קבוצות מ-audience (regex `קבוצות|פעמים`) בטופס החוג — הצעה אוטומטית בלבד, לא דריסה.
18. **פריט 4** — "ייצוא בפורמט הייבוא" למשפחות/תומכות (round-trip כמו של העיניים מ-P0.4): ייצוא CSV באותן עמודות שהייבוא מקבל.
19. **פריט 19** — armDel: החלף window.confirm במחיקות בדפוס שני-קליקים-עם-פקיעה 3.5ש׳ (המנגנון של punchConfirm — לחלץ hook משותף `useArmed`). דגל `shell.armdel`.
20. **פריט 18** — קיצורי "⬆ ייבוא" + "✓ בדיקת נתונים" חוזרים למסך המשפחות (ניווט להגדרות/סקשן) מאחורי דגל `families.shortcuts`.
21. **סטיית P2 פתוחה** — input עיר בטופס התומכת (`Supporter.city` כבר במודל ובייצוא).

## אשכול ג׳ · אימותים (פריטים 17, 20 — לא קוד, דוח)
22. **פריט 17** — השווה את כרטיס המשפחה מול רשימת ה-detail בלגאסי (הפריט המלא ב-gaps.json) — כל תת-יכולת: קיימת/סגורה/חסרה. חסרה ⇒ סגור במקום אם קטן, אחרת דווח.
23. **פריט 20** — השווה את כלל ציון-130 והצעות-6 של הפלטה מול הלגאסי (grep `score` באזור הפלטה בלגאסי) — יישר או תעד שקילות מנומקת.
24. **פריט 16** — תעד ב-gaps.json: מרכז-הטיפול closed_by="ווידג'ט משופר + 📌 מהביקורת (פער 20+22) — צורת השדרוג שאושרה; מסך ייעודי לא נבנה בהחלטת ארכיטקט".

## סיום — 129/129
25. עדכן gaps.json (הכול closed/present) · CLOSED-P3 קצר · עדכן CLAUDE.md (באגים/דגלים) · דוח מסירה. **DoD: אפס פריטים ללא סטטוס סופי.** סוויטות דפדפן: אחרי אשכול ב׳ ובסוף.
