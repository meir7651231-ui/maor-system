# ✅ CLOSED-SHOP6 — גל ה: חלוקה המונית · מלאי נכנס · רשימות המתנה

**בנאי · 30.7.2026 · ענף:** `claude/what-do-you-see-bcxttj` · מנדט: `BUILD-ORDER-SHOP6-2026-07-30.md` (גל D בתוכנית-האב). הרציונל: ראש השנה בעוד ~6 שבועות — הכאב התפעולי של חלוקת-חג ומלאי קודם לכול.

## מה נמסר (4 קומיטים)

| קומיט | תוכן |
|---|---|
| `חנות 25 ·` | **מלאי נכנס:** ‏ShopIntake (קידומת `shn`; ‏shopIntakes — **מערך הישויות ה-18**) — קנייה/תרומה-בעין; ‏addShopIntake אטומי (רשומה + העלאת מלאי באותו setDb; שער לפני מונה — לקח באג-5); מחיקה מחזירה מלאי קטום ב-0 (יומן, לא סדרת מס); StockModal הפך זרימת-קליטה (kind/source/cost, תרומה⇒cost=0); IntakePanel (יומן + סה"כ עלויות); **ShopItem.minStock** + ‏needsCare ‏`restock` ("להצטייד: נותרו N מתחת ל-M"); מיגרציה + cloud (18 אוספים) |
| `חנות 26 ·` | **חלוקה המונית:** ‏eligibleFamilies טהור (פעילות; קריטריונים כאיחוד-שיוכים; בלי כפל-חבילה); ‏bulkAssignShop (setDb יחיד, nextId פר-שורה); ‏bulkRedeem — מימוש לכל פעיל שטרם קיבל (מתנת-חג פר-חג ושנה עברית), ‏paid=0 קבוע בלי S- (גבייה פרטנית = מימוש בודד); **הכול-או-כלום: מחסור מלאי ⇒ "חסרות K יחידות" לפני כל כתיבה**; ‏distributionListLines ("☐ נמסר", דפוס תדפיס-הרכז, shop.export); UI: ‏BulkAssignModal ‏('👥 שיוך המוני', useArmed) + על חבילה '🖨 רשימת חלוקה' ו-'🎁 סימון חולק לכולם' |
| `חנות 27 ·` | **רשימות המתנה:** ‏ShopItem.waits (famId/date/note) + ריפוי מיגרציה + ‏cloud-merge LIST_FIELDS ‏shopItems:['waits']; ‏addShopWait (כפול נדחה) / removeShopWait; הצעה אוטומטית "⏳ להוסיף לרשימת ההמתנה?" במימוש (RedeemModal) ובשיוך (AssignmentForm) כשהמלאי 0; ‏needsCare ‏`waitingRestocked` — "N ממתינים ל<פריט>, המלאי חזר" (במלאי 0 אין התרעה); שורת ממתינים בפאנל הפריטים |
| `חנות 28 ·` | סגירה — e2e (קליטה +5⇒נותרו 10 · שיוך המוני ל-2 משפחות · רשימת חלוקה יורדת · חולק-לכולם⇒נותרו 7 ומומשו 1/2), שלוש הסוויטות, ידע |

## אימות (DoD)

- **verify מלא ירוק** — ‏822 בדיקות / 122 קבצים.
- **שלוש הסוויטות ירוקות:** toggle-matrix (5 פרופילים + קיצון; ‏10 בדיקות SHOP6 חדשות) · demo-walkthrough ‏100/100 · launch-readiness ‏13/13.
- **הבידוד הכספי ללא שינוי:** קליטות/חלוקה כותבים רק ל-shopIntakes/shopItems/shopAssignments; המונים הכספיים לא זזים (נבדק במפורש); source בקליטת-תרומה = טקסט חופשי, **לא** קישור ל-supporters (חיבור מקורות-מימון = גל G, בהכרעת בעלים).
- **‏bulkRedeem לעולם לא כותב חלקית** — הכול-או-כלום מוכח בבדיקה (`db זהה לחלוטין` על מחסור).

## עדכוני ratchet מתועדים (מהמנדט)

1. ‏shop-restock (חנות 13): המודאל עבר מ-upsertShopItem ישיר ל-addShopIntake — אותה תוצאה + רשומת יומן.
2. ‏shop-items (חנות 17): ‏ENTITY_COLLECTIONS ‏17→18 (‏shopIntakes).

## הכרעות בנאי (ניתנות לדריסה)

- ‏waitingRestocked נדלק גם לפריט בלי-מעקב (rem=null) עם ממתינים — "בלי מעקב" = זמין; רק rem=0 משתיק.
- ההצעה האוטומטית בשיוך מופיעה אחרי בחירת משפחה, לכל רכיבי-החבילה שאזלו (הוספה מרוכזת).
- הכפתורים על החבילה מוצגים רק כשיש שיוכים פעילים; '🖨 רשימת חלוקה' מגודר shop.export (עקבי עם CONNECT 6).
- **סוגר את שאלת minStock מ-CLOSED-UX** (סעיף 3 שם): השדה נוסף כאן במנדט — צ'יפ 'נמוך' ב-filterItems נשאר על סף ≤2 קבוע (סינון מהיר) בעוד ההתרעה restock עובדת פר-פריט לפי minStock. אם רוצים שהצ'יפ יכבד minStock — שדרוג קטן עתידי.

## שאלות פתוחות

אין שאלה חוסמת. (תזכורת מהגלים הקודמים: שאלת הלקוח החי — PLATFORM ד5 — עדיין ממתינה להכרעת בעלים.)
